# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Added
- **MultiDataset (DL3DV + DynamicVerse 통합 학습)** 데이터 경로: `src/dataset/dataset_multi.py`(`MultiDataset`+`MultiDatasetCfg`, weighted sample-level mixing, sub별 view_sampler 유지) + `src/dataset/__init__.py` 등록(union/DATASETS/get_dataset multi 분기 + `_parse_sub_cfg` name별 수동 dacite 파싱: union-in-list 모호성 회피) + `config/dataset/multi.yaml`(DL3DV[bounded,unscaled,force_empty_text]+DynamicVerse[caption_window], 둘 다 num_context12/num_target10/offset1→tgt 37(4N+1)/ctx256x448/tgt480x832 일치) + `data_module.val_dataloader` multi 분기(sub별 자기 eval index, max_batches 반). 검증: dacite 파싱·sub 빌드·**mixed batch collate**(DL3DV text="" + DynamicVerse prompt, 동일 key/shape) PASS. DL3DV 7562 + DynamicVerse 2079 scene.
- `src/dataset/dataset_dl3dv.py` `force_empty_text`(기본 false) 추가: true면 항상 `sample["text"]=""` emit (load_prompts override) → MultiDataset에서 DynamicVerse와 schema 통일(collation), DL3DV는 카메라/scene grounding만 담당.
- `scripts/mp4_to_gif.py` — mp4(파일/디렉토리 재귀) → 동일 위치 .gif 변환 (imageio 읽기 + PIL 저장, source fps 유지·`--fps`/`--overwrite` 옵션). 기존 viser_generate 출력 일괄 변환용.
- `scripts/visualize/viser_server.py`: **Generate video** 시 mp4와 함께 **gif도 저장**(`generated.gif`) — 모듈 함수 `save_gif(images, path, fps)`(PIL, loop=0) 추가. gif 실패해도 mp4는 정상 저장.
- **viser 서버 저장된 .pt target pose 로드** (`scripts/visualize/viser_server.py`): 패널에 `target .pt path` 텍스트 + **Load target .pt** 버튼 추가 — 특정 경로의 .pt를 읽어 target camera로 로드. dict면 `target_c2w_edited`(Save edited/generate `poses.pt` 포맷) / `target_c2w`(bundle) / `poses` / `c2w` / `extrinsics` 키 순으로 탐색, 단일 텐서 dict나 raw tensor/ndarray((N,4,4) 또는 (4,4))도 지원. bundle frame(context[0] 기준 relative) 가정. 로드한 pose는 pattern과 공유하는 `_set_target` 헬퍼로 target frustum/path 교체 + `tgt_poses`/`tgt_orig` 갱신(이후 gizmo 편집·Generate가 이 위에서 동작). 먼저 bundle Reload 필요(context+intrinsics용). 검증: save/generate/bundle/raw/단일/numpy 6포맷 + 실제 저장된 poses.pt 파싱 PASS.
- **viser 서버 preset target-camera 패턴** (`scripts/visualize/viser_server.py`): gizmo 수동 편집 전에 8가지 패턴으로 target camera trajectory를 생성하는 패널 추가 — dropdown `pattern`(move_forward/back/left/right, rotate_up/down/left/right) + `translate amount`(number) + `rotate deg`(number, 기본 30) + **Apply pattern** 버튼. context[0](bundle frame 기준 identity)에서 시작해 T(=현재 bundle target 수)프레임에 걸쳐 선형 ramp로 이동/회전하는 OpenCV c2w trajectory 생성(translate: 카메라 local ±X/±Z, rotate: local X(pitch)/Y(yaw) 축). **translate amount 기본값 = context view 간 최대 pairwise 거리**(bundle 로드 시 자동 계산해 필드에 채움), rotate 기본 30° — 둘 다 패널에서 수정 가능. Apply 시 기존 target frustum/path 제거 후 새 trajectory로 갱신하고 `tgt_poses`/`tgt_orig` 교체(이후 gizmo 편집·Reset·Generate가 이 패턴 위에서 동작). smoke PASS: 4패턴(move_forward/left, rotate_right/up) 생성 (va-wan_dl3dv, amount=4.67, 34프레임).
- **viser 서버 in-server video 생성** (`scripts/visualize/viser_server.py`): 서버 기동 시 `va-wan_dl3dv_256x448.ckpt`(config `custom/scenetok_va-wan_shift8_dl3dv_finetuned_wide`)를 **한 번 로드해 상주**시키고, 패널의 **"Generate video"** 버튼으로 현재 bundle의 context + (편집된) target pose를 입력으로 영상을 생성 → `results/viser_generate/<scene16>_<ts>/generated.mp4` + `poses.pt` 저장. 좌표계 처리: 편집 pose는 bundle frame(=context[0] 기준 relative, `transform@GL2CV`)인데 dataset `load_metadata`+`convert_poses`+`preprocess_batch(index=0)`도 **동일 frame**(scene rescale/recenter 없음)임을 확인 → `edited_abs = ctx0_abs @ edited_rel`로 world 복원 후 `preprocess_batch`가 재-relative화(infer_orbit의 absolute→relative 흐름과 동일). context는 **evaluation view sampler**(같은 eval index `dl3dv_c16_37_caption_standard.json`)로 dataset에서 bundle의 16 context를 정확히 재현(256x448 crop+intrinsics+reference frame). 생성 시 context frame 정합 sanity check(`max|bundle-dataset|`) 로깅. 37 target → Wan VAE temporal로 10 latent frame. wrapper는 실험 본래 bounded sampler 유지(`generate_batch_with_scene`가 `chunk_index_gap` 등 참조), loader만 evaluation으로 분리(`build_model`이 두 cfg compose). `--no_model`로 순수 시각화. smoke PASS: 원본 target 재현 시 context/target round-trip 오차 0.00000, GENERATED (1,37,3,256,448).
- `model.denoiser.scene_parallel_ca` (기본 false) + `scene_input_type="parallel_cross_attention"` + 셸 `train_ti2vgen_dynamicverse_controlnet_dynamic_no_lora_parallelca.sh`: scene CA를 text CA와 **sequential(text→scene)** 대신 **IP-Adapter식 parallel**(둘 다 self-attn 직후 같은 base를 query → residual 합산)로 (variant A; Q projection은 text/scene 여전히 별도). `scene_parallel_ca=true`면 scene_input_type=controlnet의 ctrl 블록 CA topology만 parallel로 전환, 나머지·파라미터수 동일(475M). zero-init scene proj 유지로 init 동작 보존. smoke 2step PASS. (`src/model/denoiser/wan_ti2v.py` NewDiTBlock.forward 분기 + build 3사이트)
- ReCo recon-우선 phase 옵션: `model.denoiser.freeze_reco`(기본 false) + `dynamic_loss_weight`(기본 1.0) + 셸 `train_reco_dynamicverse_reconly.sh`. `freeze_reco=true`면 ReCo DiT/VACE LoRA까지 freeze → **LightningDiT ctrl branch + ldt2reco_proj만 학습**(trainable 822M→559M). loss `recon_w·recon + dyn_w·dynamic`에서 `dynamic_loss_weight=0`으로 **recon-only**. 동기: recoX(joint) recon이 약해 dynamic까지 망가짐 → ReCo 고정+LDT만 recon에 fit해 recon 먼저 개선, dynamic은 이후 phase. recoX `last.ckpt`에서 weight 승계(resume=false). (`reco_wan_vace.py` cfg+trainable, `t2v_wrapper.py` loss)
- `model.denoiser.controlnet_inject_interval` (기본 1) + 셸 `train_ti2vgen_dynamicverse_controlnet_dynamic_no_lora_vace_int2.sh`: camera_input_type=`controlnet`(parallel) 모드에서 ctrl 잔차를 main block 0..N-1 연속이 아니라 **interval k로 [0,k,2k,…] 전체 분산 주입**(VACE vace_layers 패턴). k>1이면 ctrl block을 각 target main block에서 clone하고 수는 ceil(n_blocks/k)로 자동(ac3d_num_layers 무시). 기본 k=1은 기존 동작 유지(`ac3d_inject_ids=[0..N-1]`). int2 → Wan 30블록 중 15곳 주입, trainable 3.34B. (`src/model/denoiser/wan_ti2v.py` build+forward)
- **camera probe AC3D 충실도 개선** (`probe_wan_camera_re10k.py`/`probe_cogvideox_camera_re10k.py`): 기존엔 feature를 공간+**시간까지 global-pool**(영상당 1벡터)했는데, AC3D는 **프레임별 representation을 유지**해 unroll함 → (1)hook feature를 `(f, dim)`로 **프레임별 spatial avg-pool**(시간축 보존), (2)train-fit **PCA(dim→512)** 후 unroll → `(f×512)` feature, (3)noise를 AC3D σ **8단계**`[1/8…1]`, (4)ridge λ search `1e0–1e5`로 확장. 가장 큰 차이(시간 정보 손실)를 교정. 해상도는 `--hw`로 native 유지(Wan 저해상 깨짐).
- `scripts/eval_probe_traj_evo_allcells.py` — 저장된 probe 예측의 **모든 (block×σ) cell**에 evo ATE/RPEt/RPEr(Sim(3) 정렬) 계산 → 모델별 패널 + σ 라인 곂친 그래프(`results/probe_compare/per_model_evo_metrics.png`) + `traj_evo_allcells.json`. 모델 재실행 없이 `probe_weights.pt`의 pred 재사용.
- `scripts/eval_probe_traj_evo.py` — DynamicVerse/SINTEL식 trajectory 평가(evo: Sim(3) 정렬+scale 보정 후 ATE/RPEt/RPEr, `dynamicBA/eval_pose.py` 방식)를 저장된 probe 예측(`probe_weights.pt`의 pred)·GT에 적용. 모델별 best-rot cell에서 test scene 평균 ATE/RPEt/RPEr 출력 → `results/probe_compare/traj_evo_table.{json,png}`. (evo 설치 필요: `pip install evo`.)
- `scripts/probe_cogvideox_camera_re10k.py` — `probe_wan_camera_re10k.py`와 동일 probe(global-pool→ridge→joint 49×6 trajectory, re10k, 3-split)를 **CogVideoX-5b**(diffusers `THUDM/CogVideoX-5b`, AC3D backbone 계열)에 적용 → 동일 dataset·target·readout로 **backbone만 다른**(CogVideoX vs Wan2.2) 모델 비교. CogVideoX 특이점 처리: 49프레임 native, 16ch VAE(/8,/4, `enable_tiling/slicing`), RoPE(`_prepare_rotary_positional_embeddings`), DDIM scheduler `add_noise`(σ→timestep round(σ·999)), block이 `(video_hidden, enc_hidden)` 반환 → hook `out[0]`=video token, 480×720 native. cuDNN SDPA backend 비활성화(`enable_cudnn_sdp(False)` — CogVideoX attention에서 "No valid execution plans" 회피).
- `scripts/probe_wan_camera_re10k.py` — AC3D(arXiv:2411.18673) 충실 재현 camera linear probing (Wan2.2 TI2V-5B, RealEstate10K). 논문 본문 방법 그대로: pixelSplat `.torch` re10k 49-frame clip → 각 DiT block×noise level에서 activation **global-pool**(영상당 1벡터) → linear ridge로 **전체 trajectory**(num_frames×6 = Euler pitch/yaw/roll + frame0-relative translation) joint 회귀. high-noise(0.9/0.96, AC3D는 카메라가 t≈0.96에 형성) 포함, 전 layer(hook), train/val/test 3-split(λ는 val rot, test 보고). geodesic rot deg + trans L2 + raw-target MSE(AC3D ≈0.025 rot/≈0.48 trans와 비교) vs predict-mean baseline. dl3dv perframe probe와 달리 데이터셋·target 형태를 AC3D에 맞춰 **순수 모델 차이(Wan vs AC3D VDiT)** 격리. (AC3D 분석 대상은 자체 11.5B VDiT(CogVideoX VAE), 공개 ControlNet은 CogVideoX-2B.)
- `scripts/gen_camera_motion_t2v.py` — `prompts/camera_motion_t2v.json`의 camera-motion 프롬프트들로 Wan2.2 TI2V-5B pure-T2V 영상을 배치 생성 (fp32 pipeline 1회 로드 후 loop, `diffsynth_pure_t2v_fp32.py` 패턴 재사용). 각 결과는 `results/camera_motion_t2v/<id>/`에 mp4 + `prompt.txt`(text input) + `gen_config.json` 함께 저장. `--ids`로 subset, `--steps/--num_frames/--cfg` 조정. 용도: Wan T2V가 text로 카메라 모션을 실제 렌더링하는지 확인 (camera linear probing null의 generation-setting 후속 점검).
- `prompts/camera_motion_t2v_variants.json` / `camera_motion_t2v_variants2.json` — camera-motion 타입별 scene-varied 변형 프롬프트. 카메라 지시 문장은 원본(`camera_motion_t2v.json`)에서 verbatim 유지하고 scene/subject만 변경, 타입당 2개씩. variants=강한 3종(crane_up/tracking/handheld), variants2=나머지 9종(dolly in/out, pan L/R, tilt_up, orbit, drone, zoom_in, descend). 카메라 모션이 scene 내용에 robust한지(특정 scene fluke 아닌지) 확인용.
- `prompts/camera_motion_t2v.json` — Wan2.2 TI2V-5B T2V로 카메라 움직임이 렌더링되는지 확인하기 위한 camera-motion 테스트 프롬프트 12개 (dolly in/out, pan L/R, tilt up, orbit, crane up, tracking, drone flyover, zoom in, handheld walkthrough, top-down descend). 각 항목은 `prompt`(text input) + `id` + `camera_move` 라벨을 함께 저장. mostly-static scene + dominant camera move로 구성해 카메라 모션을 isolate. `scripts/diffsynth_pure_t2v_fp32.py --prompt` 입력으로 사용 (공통 `negative_prompt` 포함).
- `scripts/probe_wan_camera_dl3dv.py` — AC3D-style camera linear probing on the Wan2.2 TI2V-5B video DiT, using DL3DV. Wan2.2 VAE(48ch) encode → forward-hook 각 DiT block(30개) hidden state 추출 → 각 noise level에서 rectified-flow noise `(1-σ)·lat+σ·noise` (timestep=σ·1000) 주입 forward → 프레임별 feature pooling(global mean 또는 `--pool_grid G`의 G×G grid pooling, 공간 layout 보존) → z-score 표준화 + train-fit PCA(`--n_pc`) → closed-form ridge(λ search) → reference-relative pose(6D rot[col0|col1] + 95퍼센타일 정규화 translation) 회귀. geodesic rotation error(deg) + translation L2 + train-error/predict-mean baseline 동반 출력, layer×noise heatmap. **canonical linear-probe 모드 (AC3D 충실 재현)**: PCA 제거(`--n_pc` 기본 0 = raw 3072-dim feature + weight-decay ridge), train/val/test **3-split**(λ는 val로 튜닝·**test로 보고** → leakage 제거), `--pose_target relative`(인접 latent 프레임 간 상대 pose, 기본값) vs `absolute`(frame0-relative). 기본 pooling도 global mean(`--pool_grid` 기본 1)로 환원. **결과(200 scene, train/val/test=120/40/40): null 재확인** — TEST rotation 55.7–63.6° ≈ baseline 55.6° (모든 cell에서 λ=100 최강 정규화 선택), translation 0.63–0.66 > baseline 0.618 (오히려 나쁨), layer/noise flat. → null이 pooling(global/grid)·conditioning(off/in-dist)·probe(PCA/raw)·pose target(absolute/relative)·split(2/3-way) **5개 축 모두에서 robust**.
- `--in_distribution` 플래그: native TI2V 모드 (frame0=clean conditioning latent + real DL3DV prompt + `fuse_vae_embedding_in_latents=True`, per-token timestep frame0→0/rest→σ·1000); 기본 off = 기존 off-distribution(empty text, 전 프레임 noise) 동작 유지. **결과(150 scene, DL3DV): camera 신호 거의 없음 (robust null)** — val rotation 66–70° = baseline 66°, translation 0.42–0.47 vs 0.45, layer/noise 무관하게 flat. off-distribution↔in-distribution, global mean↔4×4 grid pool **4개 조합 모두 동일**. AC3D(mid-layer rot ~10–40°)와 불일치. probe 자체는 정상(6D layout 수정 후 train rot 14–48°까지 in-sample fit). → Wan2.2 TI2V-5B feature에는 scene-generalize되는 linearly-decodable camera pose 정보가 없음. AC3D와의 차이는 model(CogVideoX)·방법(relative-pose target / non-linear head 가능성) 차이로 추정.
- **ReCo 통합 완성 (#34/#35, smoke PASS)**: T2VWrapper `_reco_training_step` 분기 + config/셸. `RecoWanVace1_3BDenoiser`이면 width-doubled dual-loss 경로:
  - main 16ch ReCo latent (Wan2.1 VAE, /8 → 60x104): 좌=`inpaint_result`(recon) / 우=`video_input`(dynamic) width-concat → (B,V,16,H,2W).
  - bg 48ch latent (Wan2.2 VAE, /16 → 30x52): `inpaint_result`을 denoiser 내부 `bg_vae`(Wan2.2)로 인코딩 → same-t noise → ldt branch 입력. `_forward`가 bg를 ldt grid로, ldt 출력 ref를 ReCo grid로 bilinear resize (두 VAE downsample 다름).
  - 출력 좌/우 split → `recon_loss_weight·recon + dynamic` rectified-flow loss (loss는 ReCo 출력만).
  - denoiser에 `encode_text_condition`(raw T5, model_fn이 text_embedding 적용하므로 미투사) + `encode_bg`(dtype cast) + frozen `bg_vae`(Wan2.2 48ch) 추가. `camera_input_type=controlnet_lightningdit`로 `_derive_camera_shapes`가 camera를 latent grid([60,104])로 설정.
  - config `scenetok_reco_dynamicverse.yaml` (target ae=wan16/Wan2.1, denoiser=reco_wan_vace_1_3b) + `config/model/denoiser/reco_wan_vace_1_3b.yaml` + `config/model/autoencoders/target/wan16.yaml` + 셸 `train_reco_dynamicverse.sh`. smoke 1-step PASS (loss 1.14 = recon .60 + dyn .55).
- `reco_wan_vace_1_3b` denoiser routing 등록 (`src/model/denoiser/__init__.py` DENOISER dict + `src/main.py` T2VWrapper 분기). `_forward` 구현: LightningDiT ctrl branch(48ch background latent same-t denoise) → `ldt2reco_proj`(48→16) → `_build_vace_context`(96ch: 좌 render/recon mask0, 우 dynamic mask1) → `model_fn_wan_video`(ReCo VACE+DiT) → width-doubled pred. forward smoke 검증: ReCo path(vace_context 96ch + model_fn + width-doubled + backward) PASS (out shape (B,16,F,H,2W)). LightningDiT branch는 Wan-chunked view layout(4N) 필요 — wrapper/dataset이 공급(기존 controlnet_lightningdit과 동일 제약).
- `src/model/denoiser/reco_wan_vace.py` (**WIP**) — ReCo(Wan2.1 VACE 1.3B) denoiser + LightningDiT ControlNet branch. `RecoWanVace1_3BDenoiser`: 메인 diffsynth로 ReCo DiT(1.3B, in_dim=16) + VaceWanModel 로드, DiT/VACE에 LoRA(rank128) inject 후 ReCo pretrained LoRA(`2026_01_16_v1_release.ckpt`, DiT 600 + VACE 300 키 100% 매칭) 적용, LightningDiT ctrl branch(va-wan_dl3dv 48ch warm-start) + `ldt2reco_proj`(Conv3d 48→16, zero-init) 구성. construction dry-run 검증 완료 (trainable 822M: ReCo LoRA + LightningDiT + projector; frozen base 8.66B). `_forward`는 아직 미구현(NotImplementedError stub) — `__init__.py`/`main.py` routing 미등록이라 기존 경로 영향 없음. 설계: same-t co-sampling (ldt가 background `inpaint_result` va-wan 48ch latent denoise → projector → ReCo VACE source; ReCo 16ch width-doubled 좌=recon/우=dynamic), loss는 ReCo 출력만.
- `scripts/measure_da3_colmap_scale.py` — DL3DV 여러 scene에서 DA3-정규화 카메라와 COLMAP-정규화 카메라 사이의 단일 scale 상수 α를 측정. scene별로 COLMAP c2w(첫 프레임 reference-relative, centroid 대비 95퍼센타일 거리 정규화 → v_C)와 DA3 npz(`da3/exports/mini_npz/results.npz`의 extrinsics/depth, first-frame median depth 정규화 → v_DA3)를 만들고, Procrustes 회전 정렬 후 LS scale `α = Σ(v_DA3·v_C)/Σ|v_DA3|²` + 정렬 ATE 기록. ATE 임계 초과 scene 필터 → α median/IQR/CV(robust) → α vs depth 산점도 + hold-out 검증 → CV 기준 "단일 상수 가능/불가" 판정. summary.json + alpha_vs_depth.png 저장. DA3는 precompute된 npz 사용(재추론 없음).

### Changed
- `scripts/visualize/viser_server.py` apply_pattern/reset 반복 동작 수정: (1) `_set_target`가 매 호출 고유 노드 prefix `target_v{N}` 사용 → 같은 `/target/*` 이름 remove 직후 re-add 시 viser가 업데이트를 누락해 frustum이 stale하게 남던 문제 해결(2번째 apply 미반영, move_forward인데 직전 rotate가 남아 회전돼 보이던 증상). (2) `reset`이 no-op이던 것 수정 — apply_pattern이 `tgt_orig`를 패턴으로 덮어써 reset이 현재와 동일해졌었음. reload 시 bundle 원본 target을 `S["tgt_reset"]`에 보관하고 reset은 거기로 `_set_target` 재구성(패턴/.pt/gizmo 편집 모두 undo).
- `assets/evaluation_index/dl3dv_c16_37_{standard,unseen}.json` 재생성: **context 범위(span) 36 → 64**로 확대(16뷰 균등배치 `[0,4,9,13,17,21,26,30,34,38,43,47,51,55,60,64]`), target은 **context 범위 [0,64] 안에서 랜덤하게 연속된 37프레임**(start∈[0,28], seed=0 reproducible). 기존엔 context·target 모두 span 36 동일이라 interpolation만 됐는데, 이제 context가 더 넓고 target은 그 안의 랜덤 윈도우. 두 파일 각 140 scene, 모든 target [0,64] 범위 내 검증. (DL3DV scene 전부 ≥300 프레임이라 64-span 윈도우 fit.)
- `scripts/visualize/viser_server.py` `apply_pattern`: 패턴을 **첫 target 카메라(`bundle.target_c2w[0]`) 기준 relative pose로 직접 생성**하도록 변경 (기존엔 global identity=context[0] 기준). `pose[i] = base @ delta_local(i)` — base의 local 축(±X/±Z move, local pitch/yaw rotate)으로 offset. 원본 target 카메라를 변형하지 않고 첫 target에서 시작하는 새 trajectory를 만듦 → 시각화(frustum)와 render가 동일 pose(검증: viz vs render max 2e-7) + 패턴이 첫 target의 위치·방향에서 자연스럽게 시작. bundle에 target 없으면 identity fallback. `_make_pattern_poses`에 `base` 인자 추가.
- `scripts/visualize/viser_server.py`: ckpt↔config 자동 페어링 (`CKPT_PRESETS`). `--model_experiment`/`--model_shape` 기본을 None으로 두고 `--model_ckpt` stem에서 experiment+shape 자동 결정 (va-wan_dl3dv→shift4/256x256, va-wan_dl3dv_256x448→shift8_wide/256x448). ckpt만 바꿔도 맞는 config 적용 → 256x448 ckpt를 256x256 config로 로드해 `compressor.feat_rope` size mismatch(strict=False가 size mismatch는 안 막음)로 startup crash 나던 문제 해결. unknown ckpt은 256x256 fallback + 경고.
- `scripts/visualize/viser_server.py` `generate()` reference frame을 **첫 context view(index 0)** 로 변경 (기존 `v_c//2`). target pose(bundle frame/preset pattern)는 context[0] 기준 identity로 정의되는데 생성 reference가 중앙(v_c//2)이면 pattern의 identity 시작이 context[0]-vs-context[8] 회전만큼 틀어져 "rotation 남음" 발생 → reference를 context[0]로 맞춰 pattern 첫 카메라가 진짜 identity(R편차 0.000, pos 0)가 되게 함(기존 index8: R편차 0.49 + pos offset). 모델이 random reference로 학습돼 index 0도 유효(bundle-GT PSNR 18.7 vs 19.2@v_c//2, 무시 가능).
- `scripts/visualize/viser_server.py` 기본 모델을 **`va-wan_dl3dv.ckpt` (256x256, shift4)** 로 변경 + output을 **ckpt 이름 폴더 아래로 nesting** (`results/viser_generate/<ckpt_stem>/<scene16>_<ts>/`). 기본값 3종을 함께 전환: `--model_experiment scenetok_va-wan_shift4_dl3dv_finetuned`(published) / `--model_ckpt checkpoints/va-wan_dl3dv.ckpt` / `--model_shape 256,256`. 나머지 setting(timestep_shift=4, denoiser input_shape [16,16], camera [128,128], sampling_type=random_chunked_uniform, chunk_index_gap=5, num_target_split=2, **chunk_targets=True** → target poses 17프레임 chunk로 37→34 사용)은 전부 experiment config에서 자동 적용되므로 코드 변경 불필요. scale_context_focal_by_256는 두 ckpt 모두 true(target intrinsics는 GT 사용하는 기존 수정 그대로 유효). smoke PASS: 로드 missing=0/unexpected=32(strict=False, benign), gen (34,3,256,256), PSNR 20.16dB. (va-wan_dl3dv는 954500 step 완전 학습본, 256x448은 15000 step.)
- `scripts/visualize/viser_server.py` `generate()`: 영상 생성 시 viser 3D scene **screenshot**도 함께 저장 — 연결된 각 브라우저 client의 현재 시점을 `cl.get_render(height=900, width=aspect기반, png)`로 캡처해 `<out_dir>/viser_screenshot.png`(client 여럿이면 `_client<id>.png`)로 저장. status에 `+N screenshot` 표기. 브라우저 탭이 안 열려있으면(client 0) 스킵하고 로그만 남김(영상은 정상 저장).
- `scripts/visualize/viser_server.py` `generate()` 품질 수정 (원본 target pose에서 PSNR 14.4→19.24 dB, canonical sweep 19.23과 일치): (1) **target intrinsics** — config `scale_context_focal_by_256: true`가 **context** focal만 ~15x scale(context K diag 6.8 vs target 0.455)하는데 기존엔 scaled context K를 target에 재사용해 target ray FOV가 완전히 틀렸음 → dataset GT target intrinsics 사용. (2) **reference frame** — `preprocess_batch(index=0)` 대신 **`index=v_c//2`**(중앙 context view)로, 모델 camera-ray embedding이 학습된 기준(`eval_context_view_count_va-wan_dl3dv.py`와 동일)에 맞춤. 재구성 pose 자체는 정확(world 오차 9.5e-7)했고 intrinsics+reference frame만 문제였음. timestep_shift(4/8)·inference_steps(25/50)는 품질과 무관 확인.
- `src/dataset/view_sampler/view_sampler_evaluation.py`: `__init__`이 base `ViewSampler`에서 제거된 `is_overfitting` 인자를 아직 넘겨 `TypeError`(7 args given, base는 5–6)로 깨져 있던 것 수정 — `is_overfitting` 제거해 현 base 시그니처(`cfg, stage, cameras_are_circular, step_tracker, generator`)에 맞춤. `get_view_sampler`는 이미 `is_overfitting` 안 넘김. viser in-server 생성의 evaluation-sampler loader가 사용.
- `scripts/probe_wan_camera_re10k.py` / `scripts/probe_cogvideox_camera_re10k.py`: translation **shape** 메트릭(`trans_cos` = 예측·GT translation 궤적 cosine similarity, 크기 제거한 방향 일치) 추가 + **probe weight/예측 저장**(`{out}/probe_weights.pt`: (layer,noise)별 `W`+표준화 `mu/sd`(fp16), test 예측 `pred`, 1회 `gt_test`(euler/trans/R)). 기존엔 스칼라 metric만 남기고 W·예측 모두 폐기 → 이제 probe 재적용/예측 시각화 가능. translation L2(scale-normalized)는 기존 결과에도 이미 있어 재실행 없이 시각화 가능.
- ReCo(`va-reco_dynamicverse_ldt_ctrl`) validation에 **PSNR + FVD 메트릭** 추가 (`src/model/t2v_wrapper.py`). 기존엔 영상만 로깅했음. `_reco_validation_step`이 이제 batch_idx 0뿐 아니라 `RECO_METRIC_MAX_VIDEOS`(=24)개까지 배치를 sampling해 recon(좌)·dynamic(우) 예측과 GT(`target_reco` 좌/우 디코드)를 [0,1]로 누적 → `_reco_on_validation_end`(신규, `on_validation_end` override가 ReCo면 호출)에서 `self.metric`으로 PSNR(geodesic 아님, 픽셀)·FVD를 recon/dynamic 각각 계산해 `{loader}/{recon,dyn}/{psnr,fvd}`로 로깅 후 buffer/`reset_fvd`. cap은 full-seq sampling이 배치마다 비싸 메모리·시간 제한용 (사용 개수는 `[reco-metric]` 로그로 출력 — silent cap 아님). 영상 로깅도 decode 출력을 `(x/2+0.5).clamp` [0,1]로 정규화 (기존엔 [-1,1] raw를 `log_tensor_as_video`(=`*255`)에 직접 넘겨 음수 clamp되던 것 수정). **적용은 recoX 재시작(resume) 필요** — 실행 중 프로세스는 코드 hot-reload 안 됨.
- `src/model/diffusion_wrapper.py` validation video/context logging이 `data_loader.val.batch_size`와 무관하게 항상 최대 8개 sample을 로깅하도록 변경. 기존엔 `batch_idx == 0` 첫 배치(`b`개)만 즉시 로깅 → batch_size를 1로 줄이면 video도 1개만 올라갔음. 이제 `val_vis_buffer`에 batch별로 누적(rank 0, `val_vis_num=8`)한 뒤 `on_validation_end`에서 한 번에 flush (Sampled/Original Video + Context grid, dataloader idx 0인 `standard`는 un-prefixed 패널도 함께). Context Interpolation video는 기존대로 `batch_idx==0` 유지 (per-scene 무거운 생성이라 누적 미적용).

### Added
- `dataset.scene_id` / `dataset.force_empty_text` (`src/dataset/dataset_dynamicverse.py`):
  - `scene_id: str | None = None` — DL3DV 패턴 미러. `"<subdataset>/<scene_name>"` (예: `"DAVIS/bike-packing"`) 지정 시 `_collect_scenes`가 그 scene 하나만 반환. `fast_infer_t2v_swap_dataset.py` 단일 scene 추론용 (이전엔 dynamicverse가 scene_id 필드 없어 default scene이 골라지는 버그).
  - `force_empty_text: bool = False` — `True`면 prompt file 존재 여부와 무관하게 모든 scene 통과 + `sample["text"]=""` 강제. recon/no-text 학습(scene+camera condition만)용. 기존 prompt filter 로직은 `if not force_empty_text:` 블록으로 감쌈.
- `model.denoiser.ac3d_num_layers` ablation 학습 셸 2개 + recon/no-text 학습 셸 1개 (`scripts/train_ti2v/`):
  - `train_ti2vgen_dynamicverse_controlnet_dynamic_no_lora_layer_6.sh` — base dynamic controlnet + `ac3d_num_layers=6` (default 2 → 6, AC3D 논문 비율). exp `..._2_no_lora_layer_6`, GPU 1.
  - `train_ti2vgen_dynamicverse_controlnet_dynamic_no_lora_no_text_ctrl_layer_6.sh` — `_no_text_ctrl` + `ac3d_num_layers=6`. exp `..._no_text_ctrl_layer_6`, GPU 3.
  - `train_ti2vgen_dynamicverse_controlnet_recon_no_lora_no_text.sh` — context/target 둘 다 `inpaint_result.mp4` (default video_name) + `+dataset.force_empty_text=true` → reconstruction 학습 (text="", scene+camera만). exp `..._recon_controlnet_scene_camera_2_no_lora_no_text`, GPU 2.

### Changed
- `scripts/fast_infer_t2v_swap_dataset.py`: DL3DV가 val/test에서 고정 evaluation_index를 써 `--num_context_views` override가 무시되던 문제 — batch 로드 직후 context를 첫 N view로 truncate하는 로직 추가 (`v_c > num_context_views`이면 `context[k][:, :n]`). static-camera + context=1 추론이 폴더명(`ctx1`)과 실제로 일치하게 됨.
- `scripts/fast_infer_t2v_swap_dataset.py`: 추론 시 학습 셋업과 모델 init을 맞추기 위한 override 옵션 3개 추가.
  - `--lora_disabled` — `model.denoiser.lora.enabled=False` 강제. yaml default가 `true`라 no_lora 학습 ckpt 추론 시 `base_layer`/`lora_A`/`lora_B` key mismatch (missing 1200) 발생하던 것 해결.
  - `--prompt_style` — `dataset.prompt_style` override (예: `category_first`). dynamicverse 모델이 `category.json` dynamic-object description으로 학습됐는데 추론이 `window_dict` (배경 묘사 prompts.json) 쓰던 불일치 해결.
  - `--static_target_camera` — target extrinsics/intrinsics를 `context[:, 0]`으로 전부 덮어써 정지 카메라 시점 추론.
  - **중요 버그 수정**: 위 옵션 + `--scene_input_type controlnet --camera_input_type controlnet` 없이 돌리면 모델이 yaml default(`scene_input_type=cross_attention`, `camera_input_type=None`, LoRA on)로 init돼 학습된 `ac3d_*` ctrl branch weight가 전부 unexpected(688)로 버려지고 pure-T2V 비슷한 결과가 나옴. 옵션 명시 시 ckpt 1525 keys가 missing=0/unexpected=0으로 정확히 로드됨.

### Added
- `model.denoiser.controlnet_no_text_cross_attn: bool = False` (`src/model/denoiser/wan_ti2v.py`): parallel `controlnet` 모드의 ctrl block에서 text cross-attention을 제거하는 옵션. `True`로 두면 `NewDiTBlock.from_dit_block(..., use_text_cross_attn=False)`로 ctrl block들이 instantiate되어 `cross_attn`/`norm3` 둘 다 생성 안 됨 → text는 main DiT만 처리하고 ctrl branch는 self_attn + scene_cross_attn + FFN만. AC3D-paper-strict (ctrl branch text-blind) 정신을 parallel controlnet 모드에도 적용. 기본값 False로 기존 동작 보존.
  - 셸: `scripts/train_ti2v/train_ti2vgen_dynamicverse_controlnet_dynamic_no_lora_no_text_ctrl.sh` (`scene_input_type=controlnet`, `camera_input_type=controlnet`, LoRA off, `+model.denoiser.controlnet_no_text_cross_attn=true`, GPU 0). exp_name = `va-wan-ti2v_dynamicverse_dynamic_controlnet_scene_camera_2_no_lora_no_text_ctrl`.

- `model.denoiser.litdit_recon_loss_weight: float = 0.0` + `dataset.recon_target_video_name: str | None = None` — controlnet_lightningdit joint dynamic+background reconstruction 학습. λ>0일 때 wrapper가 LightningDiT ctrl branch의 pre-gate raw prediction을 `target["recon_video"]` (보통 `inpaint_result.mp4`)의 rectified-flow velocity에 supervise. main DiT는 그대로 `target_video_name` (e.g. `video_input.mp4`) velocity 학습. **같은 noise + 같은 timestep**, target만 다른 두 path 동시 학습 — SceneTok recon 능력 보존하면서 dynamic generation 같이.
  - 데이터셋: `dataset_dynamicverse.py`가 `recon_target_video_name` 지정 시 inpaint_result.mp4 같은 indices로 sample → `batch["target"]["recon_video"]`. 같은 crop/rescale 적용.
  - wrapper: `_forward` (controlnet_lightningdit 분기)에서 zero-init gate 적용 전 ctrl_pred를 `self._last_ctrl_pred_raw`에 저장. `training_step`이 raw를 받아 `MSE(recon_pred, process_gt(recon_target_latents, noise, timestep))` 계산 후 `loss += λ * recon_loss`. wandb에 `loss/litdit_recon` 로깅.
  - 셸: `scripts/train_ti2v/train_ti2vgen_dynamicverse_controlnet_lightningdit_dynamic_no_lora_recon.sh` (`+dataset.recon_target_video_name=inpaint_result.mp4 +model.denoiser.litdit_recon_loss_weight=1.0`, GPU 2). 

### Changed
- `model.denoiser.camera_input_type=controlnet_lightningdit` (`src/model/denoiser/wan_ti2v.py`): **구조 전면 재작성**. 기존 feedback-style block-wise residual 구조를 폐기하고 SceneTok rectified flow decoder (`LightningDiT`)를 통째로 ctrl 분기로 instantiate한 후 output-level 잔차로 main Wan DiT에 합산.
  - 폐기된 모듈: `ac3d_main2ctrl` (feedback adapter), `ac3d_camera_patch_embed`, `ac3d_t_embedder`, `ac3d_cnd_proj`, `ac3d_projectors` (block-wise residual gates), block-별 ctrl 입력 합성 로직 — 모두 제거.
  - 새 모듈:
    - `self.model.litdit_branch = LightningDiT(...)` — `va-wan_dl3dv_256-480.ckpt`에서 strict=False로 full warm-start. patch_size=1 Conv2d x_embedder, plücker `pose_embed`, pose-aware `t_embedder`, 24 LightningDiTBlock (`use_qknorm=True, use_swiglu=True, use_rmsnorm=True, use_rope_3d=True`), `cnd_proj` (64→1024), `final_layer + unpatchify` 모두 활성.
    - `self.model.litdit_output_gate = nn.Conv3d(C_lat, C_lat, kernel_size=1)` — zero-init weight+bias. ControlNet residual signature: 학습 초반엔 ctrl 기여=0 → Wan main 단독 작동 → 학습 진행하며 점진적으로 gate가 학습되어 ctrl prediction이 합산.
  - Forward (wrapper `_forward`):
    1. Main: `simple_wan_video_fn(... camera_input_type="none")` — Wan DiT만 (camera 정보 main에 안 들어감)
    2. Ctrl: `self.model.litdit_branch._forward(DenoiserInputs(view=latents_v, pose=context_pose, timestep, state=raw_cond_state))` — LightningDiT 전체 pipeline 그대로
    3. `return main_pred + litdit_output_gate(ctrl_pred)`
  - 보조 변경:
    - `src/model/types.py:DenoiserInputs.raw_state` 필드 신규 추가 — cnd_proj 거치기 전 raw cond_dim=64 scene tokens. controlnet_lightningdit 모드에서 LightningDiT 자체 cnd_proj ckpt warm-start 위해 필요. 다른 모드엔 영향 없음 (default None).
    - `src/model/diffusion.py:step()`에서 raw cond_state 함께 전달 (uncond CFG path는 zero tensor in raw cond_dim).
  - 학습 셸은 그대로 사용 가능: `model.denoiser.camera_input_type=controlnet_lightningdit` + `+model.denoiser.lightningdit_ckpt_path=checkpoints/va-wan_dl3dv_256-480.ckpt`.

### Added
- `scripts/run_standard_inference.sh <exp_name> [GPU]` — TI2V ckpt 표준 추론 셋업. 6 scenes (train: `0004`+`sav_014713_manual`, test: `blackswan`+`bike-packing`, OOD DL3DV: `a4c20f6...`+`aeea987...`) × 4 text × 2 cfg = 48 mp4. ControlNet ablation + uncertainty 자동. 새 eval index 2개 동봉: `assets/evaluation_index/dynamicverse_infer_{train,val}_2scene.json`. ckpt 간 비교 가능한 고정 grid를 제공해 매 추론마다 scene/text/cfg를 수동 구성하지 않도록 함.

### Added
- `model.denoiser.camera_input_type=controlnet_ac3d` (`src/model/denoiser/wan_ti2v.py`): AC3D 논문 VDiT-CC architecture에 충실한 새 ControlNet 변종. 기존 `controlnet` (parallel) / `controlnet_feedback` (단순 양방향)과 별도 모드로 추가, 기존 모드는 그대로 보존.
  - 핵심 차이:
    - **Noise/latent은 main DiT에만** 들어감 (ac3d_patch_embedding 없음). ctrl 분기엔 latent 직접 미입력.
    - **ctrl block i 입력** = `FC_main2ctrl(main_x_i) + ac3d_cam_emb + ctrl_x_{i-1}` — 즉 매 layer마다 main의 hidden state를 ctrl이 흡수, 직전 ctrl 출력과 carry되며, camera embedding을 layer마다 더함.
    - **ctrl block 출력** → zero-init `ac3d_projectors[i]` → main_x_i에 add → main block_{i+1} 입력으로 흘러감.
    - **ctrl block 내부 구조**: self_attn → scene_cross_attn (`scene_input_type=controlnet` 시 zero-init proj) → FFN. **text cross-attn 없음** — `NewDiTBlock`에 `use_text_cross_attn: bool=True` 인자 추가, `False`면 `cross_attn`/`norm3` 둘 다 생성 안 함.
  - 새 모듈 (모두 `self.model.*`에 attach):
    - `ac3d_camera_patch_embed: Conv3d(6·td → dim, kernel/stride/padding = main patch_embedding 동일)` — ray map만 patchify해서 per-token 임베딩 생성. 모든 ctrl layer가 같은 cam_emb 공유 (per-layer 분리 X).
    - `ac3d_blocks[N]`: main 앞 N개 block 복제, `use_text_cross_attn=False`, scene_cross_attn 활성 (scene_input_type=controlnet).
    - `ac3d_main2ctrl[N]: Linear(dim, dim)` — **NOT zero-init** (forward 신호 경로).
    - `ac3d_projectors[N]: Linear(dim, dim)` — **zero-init** (ctrl→main residual gate, ControlNet signature).
  - 학습 t=0: `ac3d_projectors` zero-init → main에 더해지는 잔차 = 0 → main DiT는 vanilla Wan 그대로 작동, Wan prior 보존. `scene_cross_attn_proj`도 ctrl block 내부에서 zero-init이라 scene 영향도 초기 0.
  - 셸 사용 예: `model.denoiser.camera_input_type=controlnet_ac3d model.denoiser.scene_input_type=controlnet`. `ac3d_num_layers` (yaml `model.denoiser.ac3d_num_layers`, 기본 2) 그대로 작동.

### Fixed
- `config/experiment/custom/scenetok_va-wan-ti2v_dynamicverse.yaml`: val OOM (step 5000 validation 진입 직후 Wan VAE decode가 +18 GiB alloc 실패) 해결 — `data_loader.val.unseen` 블록 신규 추가 (기존엔 standard만 있어서 unseen이 default `batch_size=16` 상속 → 16 DAVIS scene을 한 번에 37 × 480×832 decode). 양쪽 모두 `batch_size=1`로 내리고 `max_batches: 4 → 8`로 늘려 FVD가 N≥2 sample 확보하도록.

### Added
- `scripts/eval_context_view_count_va-wan_dl3dv.py`: published `checkpoints/va-wan_dl3dv.ckpt` (`scenetok_va-wan_shift4_dl3dv_finetuned`, 256×256, dl3dv `c16_37_standard` eval index)로 context view 개수 N ∈ {1,2,3,4,8,12,16}에 따른 재구성 성능 sweep. 16-view 풀-batch를 1회 sampling 한 뒤 `slice_context`로 N개 뷰만 잘라 compressor에 넣고 같은 RNG로 denoise → mp4(`recon_N=*.mp4`)+per-scene `metrics.csv`+global `per_scene.csv`/`summary.csv`(PSNR/SSIM/LPIPS) 저장. 첫 8 scene 사용. `--context_sampling {first,uniform}`로 뷰 선택 전략 분기 — `first`는 인접한 처음 N개, `uniform`은 `np.linspace(0, V-1, N).round()`로 16뷰에 균등 분포. uniform이 적은 N에서 first 대비 +1.5~+2.6 PSNR로 우수(N=1, N=16에서는 동일).
  - `context_root`/`target_root`/`map_dict` 키 제거를 Hydra `~key` override가 아니라 compose 후 OmegaConf 조작으로 옮김 — latent-style config(키 있음) + raw-style config(키 없음) 둘 다에서 동작. 덕분에 256x448 wide variant도 같은 스크립트로 sweep 가능.
  - 256x448 sweep: `--scenetok_experiment custom/scenetok_va-wan_shift4_dl3dv_finetuned_wide --scenetok_ckpt checkpoints/va-wan_dl3dv_256x448.ckpt --context_shape 256,448 --target_shape 256,448 --context_sampling uniform`로 실행, `results/eval_context_view_count_va-wan_dl3dv_256x448_uniform/`에 저장. 동일 8 scene 기준 PSNR가 256x256 ckpt 대비 N별 −0.4 ~ −0.8 (예: N=4 18.01 vs 18.55) 낮고 N=4 부근에서 일찍 plateau (N=4→N=16 Δ +0.4).
  - `--dataset`/`--dataset_root`/`--view_sampler_td` CLI 추가 — DL3DV/RE10K 양쪽에서 동일 스크립트 재사용. RE10K raw mode에서 `unbounded` sampler의 `td=4 × target_index` OOB 버그(CHANGELOG의 stage1 항목 동일)를 `--view_sampler_td 1`로 우회. `view_sampler.num_context_views`는 `max(N list)`에서 derive (re10k=c12면 N=16은 자동 skip).
  - va-videodc_dl3dv uniform sweep (`checkpoints/va-videodc_dl3dv.ckpt` + `scenetok_va-vdc_shift4_dl3dv_finetuned`, 256x256, dl3dv c16_37_standard) → `results/eval_context_view_count_va-videodc_dl3dv_uniform/`. PSNR: N=1 14.87 / N=4 19.35 / N=12 20.26 / N=16 20.29. 동일 8 scene 기준 va-wan_dl3dv(N=4 18.55, N=16 19.08) 대비 +0.8~+1.2 PSNR 우위 (videodc decoder가 동일 scene token 수에서 더 강함).
  - va-videodc_re10k uniform sweep (`checkpoints/va-videodc_re10k.ckpt` + `scenetok_va-vdc_re10k_scratch`, 256x256, re10k c12_37_standard, `--view_sampler_td 1`) → `results/eval_context_view_count_va-videodc_re10k_uniform_v2/`. PSNR: N=1 13.76 / N=4 21.62 / N=8 21.72 / N=12 22.23 (re10k는 12-view eval index라 N=16 skip). RE10K가 DL3DV보다 N≥4부터 +1.4~+2 PSNR 높음 (forward-translation trajectory가 DL3DV의 360° orbit보다 reconstruction이 쉬움). 단 std는 5.2~6.5로 매우 큼 (e98d 같은 표지판 scene이 PSNR 33+ , 8d08 같은 흐릿한 dark scene이 8~22 폭).
  - **Set-level FID/FVD 추가**: N별 8 scene을 모두 accumulate한 뒤 `metric.update_fid/fvd` → `compute_fid/fvd(update=False)` 패턴으로 set-level metric 계산. `summary.csv`에 `fid`, `fvd` 컬럼 추가. 4 ckpt 재실행 결과 (`*_fidfvd/` dir):
    - va-wan_dl3dv (256x256, N=16): PSNR 19.07 / FID 46.74 / FVD 3808
    - va-videodc_dl3dv (256x256, N=16): PSNR 20.29 / FID 51.40 / FVD 3940
    - va-wan_dl3dv_256x448 (N=16): PSNR 18.40 / FID 45.98 / FVD 4886
    - va-videodc_re10k (N=12): PSNR 22.33 / FID 74.59 / FVD None (scipy.sqrtm non-finite, 8-sample cov 부족)
    - **PSNR ↔ FID/FVD 부호 반대 트레이드오프 관찰**: va-videodc는 모든 N에서 PSNR/SSIM/LPIPS 우위지만 set-level FID/FVD는 va-wan이 낮음(=좋음). pixel-match vs perceptual distribution match의 전형적 트레이드오프 — VideoDCAE 출력이 더 sharp한 pixel align이지만 I3D/Inception feature 공간에선 살짝 distribution mismatch가 더 큼. ⚠ 단 8-scene cov 추정 노이즈가 커서 부호 신뢰도 낮음 — 논문(140 scenes)과 비교 시 결과 뒤집힐 가능성 있음.
- `src/model/metrics.py:compute_fvd`: pred_list/gt_list가 비거나 N<2면 NaN tensor 대신 **`None` 반환** → `on_validation_end`의 `if value is not None` 가드와 통합돼 wandb에 NaN/inf 안 찍힘. scipy `sqrtm`이 rank-deficient cov에서 무한 스핀하는 CLAUDE.md gotcha 회피. 추가로 `frechet_video_distance` 결과가 non-finite여도 `None` 반환.
- `src/model/metrics.py:Metric.forward`: 모든 `compute_<key>(...)` 결과를 검사해서 scalar tensor가 non-finite면 `None`으로 변환 (PSNR/SSIM/LPIPS도 동일 가드). 한 줄 `[Metric] skip <key>: non-finite ...` console log.
- `src/model/diffusion_wrapper.py:on_validation_end`: `full_sequence_metrics` 로깅 루프에 `if value is None: continue` 가드 추가 (기존엔 NaN 그대로 wandb로 보냄, `general_metrics`엔 이미 None 가드 있었음).

### Added
- `src/dataset/dataset_dynamicverse.py` + `config/dataset/dynamicverse.yaml`: `WorldTraj/dynamicverse` 데이터셋 (DAVIS / dynamic_replica / MOSE / MVS-Synth / SAV / spring / uvo / VOST / youtube_vis, `dynpose-100k`+`logs` 제외). 한 scene 디렉터리당 `inpaint_result.mp4`+`cameras.json`+`prompts.json`. 주요 cfg 필드:
  - `excluded_subdatasets` (기본 `["dynpose-100k","logs"]`), `unseen_subdatasets` (기본 `["DAVIS"]`), `val_seen: bool` — train + val.standard는 unseen 제외 pool, val.unseen은 unseen pool만.
  - `evaluation_index_path: Path | None` — JSON `{scene_name: {context, target}}`. stage∈{val,test}이면 view_sampler 대신 이 인덱스의 fixed indices 사용 (DL3DV 패턴 미러).
  - `context_shape` / `target_shape` (둘 다 None이면 `shape` fallback). `apply_crop_shim_to_views`를 view-type별로 따로 호출.
  - `target_video_name: str | None` — 설정 시 target views가 다른 mp4에서 로드 (예: context=`inpaint_result.mp4` 배경, target=`video_input.mp4` 원본).
  - `prompt_style: "window_dict" | "category_first"` — 전자는 `prompts.json` 의 `prompt_key`(기본 `prompt_scene`); 후자는 `category/category.json`의 `reasoning[dynamic[0]]` 사용 (foreground 동적 객체 설명을 text로).
  - `require_prompt: bool` — true면 prompt 없는 scene drop (`default_collate`가 batch 내 키 불일치 시 text 키 떨어뜨리는 문제 회피).
  registry/`__init__.py` + `DatasetCfg` union 갱신. `view_sampler.sample(...)` 호출에 `scene_dir` kwarg 추가 (caption_window 지원).
- `src/dataset/data_module.py`: dynamicverse val 라우팅 추가 — val loader key가 `unseen`이면 `val_seen=False`+`evaluation_index_path=assets/evaluation_index/dynamicverse_unseen.json`, 그 외는 `val_seen=True`+`_standard.json`. DL3DV/RE10K의 기존 `val_seen` 분기 패턴과 통일.
- `scripts/build_dynamicverse_eval_index.py` + `assets/evaluation_index/dynamicverse_{standard,unseen}.json`: dynamicverse용 fixed eval index 빌더. seen pool(1329 scenes, DAVIS 제외)에서 random 32 → standard, unseen pool(DAVIS 76)에서 random 32 → unseen. Context=`[0,4,9,13,18,22,27,31,36,40,45,48]` (12 indices, 50-frame clip 가정), target=`range(37)` (Wan 4N+1, td=4).
- `config/experiment/custom/scenetok_va-wan-ti2v_dynamicverse.yaml`: dynamicverse용 Wan TI2V 5B 학습 실험. `scenetok_va-wan-ti2v_dl3dv.yaml` 미러링 + dynamicverse 측 조정 — view_sampler=caption_window(`prompts_filename=prompts.json`), context_shape=[256,448], target_shape=[480,832], compressor=256x448-finetune ckpt+frozen, scheduler/optimizer는 dl3dv와 동일. 셸 측 override를 위해 `model.denoiser.{scene,camera,condition_latents}_input_type`, `model.denoiser.lora.*`, `wandb.*` 는 yaml에서 빼고 학습 셸로 옮김.
- `scripts/train_ti2v/train_ti2vgen_dynamicverse_controlnet.sh`: dynamicverse + scene/camera 모두 parallel ControlNet (`scene_input_type=controlnet`, `camera_input_type=controlnet`) + LoRA on (rank=32) 학습 셸. context=target=`inpaint_result.mp4`, text=`prompt_scene`. GPU 1.
- `scripts/train_ti2v/train_ti2vgen_dynamicverse_controlnet_dynamic.sh`: 위 셸의 변종 — target views가 `video_input.mp4`(원본, 동적 객체 포함)에서 옴, text는 `category/category.json`의 `reasoning[dynamic[0]]`(foreground 동작 설명). scene-token=배경, text=foreground 분리 가설 검증용. GPU 0.
- `scripts/stage1_lightningdit_coarse.py`: 다수 신규 arg —
  - `--repeat_factor N` (N>1이면 N MC samples의 픽셀별 std → `variance.mp4` (viridis) + `variance_gray.mp4` (grayscale) + `variance.pt` (raw tensor) 저장; 논문 variance map 재현용).
  - `--dataset {dl3dv,re10k}` + `--dataset_root` (Hydra-compose 경로에서 dataset group/root override; published latent_dl3dv 등에서 raw 데이터셋으로 swap).
  - `--view_sampler_td N` (unbounded sampler의 `td * raw_index` OOB 버그 회피 — raw mode RE10K에서 `td=1` override).
  - `--val_seen {true,false}` (RE10K test pool 접근용).
  - `--view_sampler_group {evaluation_video,...}` + `--view_sampler_index_path` (eval-style 고정 index 사용 swap).
- `infer_scenetok_variance.sh` + `assets/evaluation_index/re10k_c12_128_93b36a.json`: paper-faithful Stage-1-only SceneTok variance map 추론 셸 (3 case: va-wan_dl3dv / va-videodc_dl3dv / va-videodc_re10k, 모두 256×256, N=8 MC sampling). RE10K 93b36a scene용 custom 1-scene index.
- `src/dataset/dataset_re10k.py`: `DatasetRE10kCfg.scene_id: str | None = None` 추가 — DL3DV의 `scene_id` 미러링. 단일 scene 추론 시 view_sampler 호출 전에 `self.scenes = [cfg.scene_id]`로 short-circuit.
- `scripts/train_scenetok/train_scenetok_dl3dv_256-480_finetune_fixed_intrin.sh` + `_no_kl.sh`: `scenetok_va-wan_dl3dv_256-480_finetune_small`의 두 변종 셸 — `_fixed_intrin`은 `derive_shape_dependent_fields` 도입 후 baseline 재학습, `_no_kl`는 `kl_weights=[0,0]`으로 scene token reparameterize의 σ를 완전 자유롭게.

### Changed
- `src/model/diffusion_wrapper.py`: 기존 `set_denoiser_input_shape_from_target` (target_shape → denoiser.input_shape 자동 derive)을 일반화한 **`derive_shape_dependent_fields`** 도입. `dataset.context_shape`/`target_shape`이 바뀔 때 6개 의존 필드가 모두 자동 갱신됨:
  - `denoiser.input_shape` = `target_shape // ae_spatial_compression(target_ae)`
  - `denoiser.camera.input_shape` = mode 분기:
    - **latent-domain** (`channel_concat`/`controlnet`/`controlnet_feedback`/`ac3d`): `latent_shape` (rays가 latent grid에서 channel-concat돼서)
    - **pixel/2-domain** (`recam_attention`/`cross_attention`/`new_cross_attention`/`adaln`/`wan_control`/`none`): `target_shape // 2` (LVSM patch embed 또는 SimpleAdapter의 내부 16x downsample 가정)
  - `denoiser.camera.embedding.patch_size` = `cam_input // latent_shape` (pixel-domain 한정, latent-domain은 yaml에 위임)
  - `compressor.input_shape` = `context_shape // ae_spatial_compression(context_ae)`
  - `compressor.camera.input_shape` = `context_shape // 2`
  - `compressor.camera.embedding.patch_size` = compressor camera token grid이 `compressor.input_shape // compressor.kwargs.patch_size`와 일치하도록.
  변경된 값만 cyan 로그로 표시 + 일치하면 silent. 기존 22개 SceneTok exp config은 전부 일치(0 override), TI2V/T2V 46개 중 3개는 실제 yaml staleness 발견되어 자동 수정(`_256_scene_ca_recam_*`, 14B exp — target=256x256인데 yaml에 480 base 값이 stale로 남아있던 케이스). `set_denoiser_input_shape_from_target`는 back-compat 알리아스로 유지. 호출은 `DiffusionWrapper.__init__`에서 자동 (기존 위치 그대로). KNOWN_BUGS의 `scale_*_focal_by_256` legacy bug-compat 플래그는 의도적으로 건드리지 않음 (학습된 ckpt 호환 깨질 위험).

### Added
- `scripts/eval_compare_256upscale_vs_480.py`: SceneTok lightningDiT 두 ckpt(`scenetok_va-wan_dl3dv_480_finetune_large`와 `scenetok_va-wan_dl3dv_256_finetune_large`)를 DL3DV `standard`/`unseen` evaluation index(`assets/evaluation_index/dl3dv_c16_37_<split>.json`) 140 scene씩 양쪽에서 비교 평가하는 스크립트. 256 ckpt 출력은 `F.interpolate(mode="bilinear")`로 480×832까지 upscale 후 480 GT와 비교. `DiffusionWrapper`를 `.hydra/config.yaml`+`last.ckpt`에서 직접 instantiate(stage1과 동일 패턴), 각 case마다 model의 native target_shape로 sampling → GT용으로는 `target_shape=[480,832]`로 override한 별도 데이터셋 인스턴스를 평행 iterate(같은 eval_index + val_seen이면 chunk 순서 deterministic). 메트릭은 `DiffusionWrapper.metric`(공유 `Metric()`) 재사용해서 학습-time validation과 동일 dtype/구현 — per-scene + dataset-level PSNR/SSIM/LPIPS와 dataset-level FID/FVD. 출력: `results/eval_compare_256upscale_vs_480/per_scene_<case>_<split>.csv` + `summary.json` (각 case 완료 직후 stream 저장).

### Changed
- `scripts/stage2_reco_edit.py`: `--upscale` flag 추가 (default off). off면 coarse mp4의 native (H, W)를 그대로 ReCo에 넘기고 `--height/--width`는 무시; on이면 bilinear interpolation으로 `(--height, --width)`까지 upscale한 뒤 ReCo 통과 (이전 default 동작). `read_mp4_frames`가 `(tensor, h, w)` 튜플 반환으로 변경되어 main이 실제 사용 (h, w)를 받아 ReCo `pipe(height=..., width=w*2, ...)`와 `build_reco_inputs`에 전달.
- `infer_lightningdit_reco.sh`: `run_pipeline` 함수로 묶어 **4 케이스 sequential 실행**으로 재작성. 출력 디렉터리에 해상도 tag suffix(`_upscale480` / `_native256` / `_native480` / `_vavdc_native256`)를 붙여 충돌 회피.
  - (1) `scenetok_va-wan_dl3dv_256_finetune_large` (shape 256×448) + `--upscale` → ReCo at 480×832
  - (2) 동일 exp, no upscale → ReCo at native 256×448
  - (3) `scenetok_va-wan_dl3dv_480_finetune_large` (shape 480×832), no upscale → ReCo at native 480×832
  - (4) published `scenetok_va-vdc_shift8_dl3dv_finetuned` + `checkpoints/va-videodc_dl3dv.ckpt` (shape 256×256, no exp dir → Hydra-compose 경로), no upscale → ReCo at native 256×256
- `scripts/stage1_lightningdit_coarse.py`: `--scenetok_experiment <name>` arg 추가 — `exp/<name>/.hydra` dump이 없는 published config을 Hydra `initialize_config_dir`+`compose`로 in-process 빌드 (`+experiment=<name> mode=test wandb.activated=false`). 이때 published va-vdc config이 `latent` 데이터셋용 `context_root`/`target_root`/`map_dict`을 `dl3dv` block에 박아놔서 dacite가 unknown field로 거부할 수 있으므로 `~dataset.<field>` 삭제 override 추가, `dataset.root=./DATA/DL3DV/DL3DV-960`도 명시. 기존 `--scenetok_exp` (own training exp) 경로는 그대로 유지; 두 옵션은 mutually exclusive로 검증.
- `scripts/stage2_reco_edit.py`: ReCo 파이프라인 출력이 `(W*4) × (H*2)` 즉 **2행 4열 visualization grid** 라는 점에 맞춰 후처리 로직 재작성. 기존 `stage2_full_sidebyside.mp4` 명칭은 오해를 부르므로 `stage2_full_grid.mp4`로 rename. 기존에 "right half"로 잘라내던 `stage2_edit_right.mp4`는 실제로는 grid의 빈 placeholder 영역이었어서 제거. 대신 cell width=`fw//4`, cell height=`fh//2` 기반으로 **2행 1+2열만 추출**: `stage2_input_output.mp4` = 2행 1열(input coarse) + 2열(edit result) 가로 concat (`2*cell_w × cell_h`), `stage2_edit.mp4` = 2행 2열만 (edit result; `cell_w × cell_h`). 기존 산출물(`results/lightningdit_reco_scenetok_va-wan_dl3dv_480_finetune_large/<scene>/`)도 ffmpeg crop으로 in-place 재생성 (`stage2_full_grid.mp4` 3328×960 → `stage2_input_output.mp4` 1664×480, `stage2_edit.mp4` 832×480, 모두 81f).

### Added
- `src/model/denoiser/wan_ti2v.py`: `camera_input_type="controlnet_feedback"` 모드 추가 — AC3D 페이퍼 (VDiT-CC) 의 bidirectional ControlNet 구조 (공개 AC3D 코드 = parallel과 다름). 기존 `controlnet`과 동일한 `ac3d_patch_embedding`/`ac3d_blocks`/`ac3d_projectors` 모듈에 더해 **`ac3d_fc_down: nn.ModuleList[nn.Linear(dim,dim)] × N` (zero-init)** 추가 → 매 layer마다 main DiT hidden state를 ac3d branch로 feedback. `simple_wan_video_fn`은 모드 detection (`ac3d_feedback_mode`)으로 분기: feedback 모드면 main loop **안에서** interleaved 실행 (`ac3d_x = ac3d_blocks[i](ac3d_x + fc_down[i](main_x), ...); main_x += proj[i](ac3d_x); main_x = main_blocks[i](main_x)`), parallel 모드(`controlnet`)는 기존대로 pre-compute 후 사후 residual. zero-init 두 단계 (`fc_down`, `projectors`) 덕분에 t=0에서 vanilla Wan과 완전 동일. `_set_trainable_parameters`는 `ac3d_fc_down` 포함. 기존 `controlnet` ckpt도 strict=False로 load 가능 (새 `ac3d_fc_down`는 zero/random init → feedback contribution=0에서 시작 → 점진 학습).

### Fixed
- `src/model/t2v_wrapper.py:generate_batch_with_scene`: `chunk_index_gap=self.dataset_cfg.view_sampler.chunk_index_gap` 직접 접근을 `getattr(..., "chunk_index_gap", 1)`로 변경. `caption_window` sampler cfg엔 `chunk_index_gap` 필드가 없어 (bounded/unbounded 전용) val/sanity-val에서 `AttributeError` 발생하던 문제. caption_window는 `chunk_targets=False, offset=1`이라 downstream index 계산이 no-chunk 경로(=`chunk_index_gap` 무시)를 타므로 default 1이 결과에 영향 없음. 바로 아래 `chunk_targets` getattr와 동일 패턴. (주의: `model.mask_context=true`면 line 519의 `view_sampler.min_context_views`, `scheduler.sampling_type=random_chunked_uniform`이면 line 563/580의 `num_target_split`도 caption_window에 없어 동일 가드 필요 — 현재 config는 둘 다 비활성이라 미발생.)
- `src/model/metrics.py:compute_ssim`: validation 끝에서 SSIM이 GPU OOM 나던 문제 (172 GB 점유 + SSIM intermediate 5+ GB 필요 → 2.79 GB free에 안 들어감). N×3×H×W 전체에 대해 한 번에 호출하던 것을 chunk=8로 분할 loop으로 변경. chunk별 intermediate ~200-400 MB로 줄여서 free 메모리에 fit. 결과는 sample 수 가중 평균 (이전 single-call의 batch 평균과 수학적 동일).
- `src/model/denoiser/wan_ti2v.py` `channel_concat` patch_embedding extra channel 수: `pose_embed(skip_embedding=True)`가 `embedding.in_channels * temporal_downsample = 6 * 4 = 24` 채널을 반환하는데 `__init__`에서 patch_embedding을 `+6`만 확장해서 conv가 `expected 54, got 72` 에러. `extra_in = 6 * max(temporal_downsample, 1)`로 수정 (wan_control의 SimpleAdapter `in_dim`과 동일 규칙).
- `src/model/denoiser/wan_ti2v.py:simple_wan_video_fn` scene cross_attention concat: text context와 scene_context를 dim=1으로 cat할 때 batch dim mismatch 발생하던 버그. CFG의 uncond branch가 `""`을 batch=1로 인코딩하는 반면 scene_context는 val/train batch (예: 8)를 갖고 있어 `RuntimeError: Sizes of tensors must match except in dimension 1`. cat 직전에 broadcast/repeat_interleave 추가 (한쪽이 1이면 expand, 한쪽이 다른쪽의 배수면 repeat).

### Added
- `src/model/denoiser/wan_ti2v.py`: `scene_input_type="controlnet"` 모드 추가 — scene tokens을 main DiT가 아니라 **AC3D ControlNet 분기**의 scene cross-attention으로 주입. camera(ray channel-concat)와 함께 분기에 들어가 분기 residual이 scene+camera guidance를 모두 담음. 구현: (a) `__init__`에서 `scene_input_type=="controlnet"`이면 `ac3d_blocks`를 `scene_input_type="new_cross_attention"`으로 deepcopy해 scene cross-attn(zero-init proj) 모듈을 갖게 함; (b) `simple_wan_video_fn` scene routing에 `controlnet` 분기 추가 → `ac3d_scene_context`로 보관(main 블록엔 미주입); (c) ac3d 분기 루프에서 `ac3d_block(..., scene_context=ac3d_scene_context, ...)`로 전달 + batch broadcast/repeat. trainable은 `ac3d_blocks.*` 규칙이 scene_cross_attn까지 커버, scene projection은 `cnd_proj`(항상 trainable). zero-init proj 덕에 t=0 vanilla Wan과 동일. **전제: `camera_input_type="controlnet"` 필요** (분기 생성용) — 아니면 `__init__`에서 `ValueError`. Literal/validation(`WanTI2VConfig` + `simple_wan_video_fn`)에 `controlnet` 추가. 셸 사용: `model.denoiser.scene_input_type=controlnet model.denoiser.camera_input_type=controlnet`.
- `scripts/build_caption_window_eval_index.py` + `assets/evaluation_index/dl3dv_c16_37_caption_{standard,unseen}.json` — caption_window용 **고정 evaluation index** 생성기와 산출 파일. 기존 `dl3dv_c16_37_{standard,unseen}.json`의 scene 목록을 그대로 재사용(같은 scene 평가)하되, scene별 고정 seed로 caption_window sampler를 돌려 target을 prompts_37.json의 한 caption window로, context를 그 window 안/주변에서 뽑아 `{scene: {context, target}}` 포맷으로 저장. 각 split 140 scenes 전부 생성(0 skip).
- `src/dataset/view_sampler/view_sampler_caption_window.py`: bounded-style **context margin warm-up** 추가. `ViewSamplerCaptionWindowCfg`에 `initial_context_window_margin`/`context_window_margin_warm_up_steps` 필드 + `_schedule`(global_step 기반 선형 ramp)/`_effective_margin` 메서드. `sample()`이 고정 `context_window_margin` 대신 effective margin 사용 → 학습이 진행될수록 context view 범위가 caption window 밖으로 점점 넓어짐 (bounded의 `context_gap` warm-up과 동일 거동). `warm_up_steps=0`이면 기존 동작(고정 margin) 유지, test stage는 항상 final margin. `caption_window.yaml`에 두 필드 노출. 검증: window `[111,148)` 고정 시 step 0→1000에서 margin 2→40, context span 40→116으로 확대 후 클램프.
- `config/dataset/view_sampler/caption_window.yaml` — `caption_window` view sampler의 Hydra group config (지금까지 코드 등록만 있고 yaml이 없어 `dataset/view_sampler=caption_window` 그룹 override가 불가능했음). 필드: `name`, `num_context/target_views`(experiment에서 override), `context_sampling`, `prompts_filename`, `temporal_downsample`, `offset`, `chunk_targets`, `context_window_margin`, `allow_short_last_window`. dacite union은 `name` Literal로 `ViewSamplerCaptionWindowCfg`에 정확히 해석되고, experiment의 bounded 전용 필드(`max_distance_*` 등)는 strict=False라 무시됨.
- `scripts/verify_caption_window_text.py` — `train_ti2vgen_recon_overfit_256_finetuned.sh`의 dataset-side override를 Hydra로 그대로 compose(+`load_typed_root_config`)해서 caption_window+load_prompts가 `sample["text"]`를 caption 구간에 맞게 채우는지 검증. 모델 미로드. 검증 결과(dl3dv smallset 5 scenes): target이 정확히 V_pixel=37 **연속** 프레임 구간이고 각 구간이 `prompts_37.json`의 한 caption window와 일치, `sample["text"] == 그 window의 prompt_scene_simple.concise`.
- `scripts/re10k_to_cameractrl_scene.py`: `--limit N`으로 **여러 scene 일괄 변환** 지원 추가 (scene별 변환을 `convert_scene` 헬퍼로 분리, `>= min_frames`인 scene을 chunk 순서대로 N개 선택해 하나의 `annotations/test.json`에 list로 누적). `--limit 1`(기본)이면 기존 단일 scene 동작 그대로. 실패 scene은 skip 후 계속. 검증: `--limit 10`으로 `re10k_multi` 생성 → ac3d env decord로 10개 mp4 프레임 수 == pose 라인 수 전부 일치, `RealEstate10KPoseControlnetDataset` 10개 다 hang 없이 로드.
- `scripts/re10k_to_cameractrl_scene.py` — scenetok pixelSplat RealEstate10K `.torch` chunk에서 scene 하나를 CameraCtrl/AC3D 데이터셋 포맷(`pose_files/<key>.txt` + `video_clips/<key>.mp4` + `annotations/test.json`)으로 변환. chunk가 jpeg frames + `cameras`(N,18) + `timestamps` + `url`을 이미 들고 있어 YouTube 다운로드 불필요. pose txt는 `Camera` 파서 매핑대로 `ts fx fy cx cy 0 0 <w2c 12>` 19컬럼, mp4는 `macro_block_size=1`로 원본 해상도 보존, decord 프레임 수 == pose 라인 수를 검증해 AC3D `__getitem__`의 `while True` 무한 retry(프레임 OOB)를 방지. AC3D 추론은 `video_root_dir=<out_root>`, `annotation_json=annotations/test.json`으로 사용. 검증: test/000000.torch의 scene `5aca87f95a9412c6`(143 frames) 변환 후 `RealEstate10KPoseControlnetDataset[0]`이 hang 없이 `video (49,3,480,720)` + plücker `(49,6,480,720)` 반환.
- `src/dataset/view_sampler/view_sampler_caption_window.py` + 등록 — caption-aligned view sampler. `<scene>/prompts_37.json`에서 random caption window (`frame_idx=[lo, hi)` half-open)를 픽해서 target indices를 `[lo, lo+V_pixel-1]`로 고정, context indices는 같은 window (또는 `context_window_margin`만큼 확장한 구간)에서 `uniform`/`farthest_point`로 sampling. `bounded`와 동일한 `(ViewIndex, raw_target)` return shape. `dataset_dl3dv.py`의 `view_sampler.sample(...)` 호출에 `scene_dir=chunk_path` 추가 (bounded/unbounded는 `**kwargs`로 흡수). `dataset.load_prompts=true`와 함께 쓰면 target 구간과 prompt가 일관됨. Cfg 필드: `prompts_filename`, `temporal_downsample`, `offset`, `chunk_targets`, `context_window_margin`, `allow_short_last_window` (마지막 짧은 caption window 허용 여부).
- `src/dataset/dataset_dl3dv.py`: `DatasetDL3DVCfg`에 `load_prompts: bool = False`, `prompts_filename: str = "prompts_37.json"` 필드 추가. `load_prompts=True`면 `__getitem__` 끝에서 `<scene>/prompts_37.json`을 lazy load해서 target frame 범위 `[min, max]`가 들어가는 entry (`frame_idx=[lo, hi)` half-open, 가장 좁은 span 선택)의 `prompt_scene_simple.concise` 텍스트를 `sample["text"]`에 주입. cache는 worker-local `self._prompts_cache` dict. 파일 없거나 매칭 안 되면 빈 문자열. T2VWrapper의 `get_text_condition`은 이미 `batch["text"]`를 읽도록 돼있어서 training/val 양쪽에서 자동으로 prompt 사용됨. 사용: shell에 `+dataset.load_prompts=true` (struct 우회 위해 `+` prefix 필요).
- `scripts/blacklist_non_consecutive_frames.py` — DL3DV train pool 9052 scenes 스캔해서 `images/frame_{05d}.png`의 frame index가 연속이 아닌 (`max-min+1 != count`) scene을 `train/blacklist.csv`에 `reason=non_consecutive_frames`로 append. 32-thread `os.scandir` 기반. detail 컬럼에 `range=lo..hi count=N missing=...`. 기존 `append_blacklist`/`load_blacklist` helper 재사용, dedup 자동 처리. 1회 실행 결과: 259개 신규 blacklist.
- `src/model/denoiser/wan_ti2v.py`: `camera_input_type="ac3d"` 모드 추가 — AC3D / CogVideoX-ControlNet 패턴으로 카메라 주입. `__init__`에서: (a) `model.ac3d_patch_embedding` Conv3d (in_ch = orig + 6*T = 72), 첫 48ch는 pretrained patch_embedding 복사 + 새 24ch zero-init; (b) `model.ac3d_blocks` = 첫 `ac3d_num_layers` (기본=2) 개 main Wan block을 `NewDiTBlock.from_dit_block(..., camera_input_type="none", scene_input_type="none")`로 deepcopy (내부 scene/recam path 비활성); (c) `model.ac3d_projectors` = N개 zero-init `nn.Linear(dim, dim)`. `simple_wan_video_fn`은 main DiT vanilla patchify는 그대로 두고, 별도로 `ac3d_patch_embedding(cat(latent, ray))` → `ac3d_blocks` → `ac3d_projectors`로 layer별 residual 산출, main block loop에서 `i < ac3d_num_layers`이면 `x[:, :n_patches] += ac3d_residuals[i]`로 주입 (scene_latent_tokens 부분은 보존). zero-init projector 덕분에 t=0 시점 vanilla Wan과 동일. `_set_trainable_parameters`는 `ac3d_*` 전부 trainable. `WanTI2VConfig.ac3d_num_layers: int = 2` 필드 추가. shell 사용: `+model.denoiser.camera_input_type=controlnet +model.denoiser.ac3d_num_layers=2 +model.denoiser.camera.input_shape=[H_latent,W_latent]` (값은 아래 Changed에서 `ac3d`→`controlnet`으로 리네임됨). AC3D 원본의 timestep gating (t ∈ [800, 1000]) 는 아직 미구현 — wrapper의 noise sampler 변경 필요시 별도.
- `src/model/denoiser/wan_ti2v.py`: `WanTI2VConfig.scene_projection: Literal["linear", "mlp"] = "linear"` 필드 추가. `mlp` 모드면 `cnd_proj`가 `nn.Sequential(Linear(cond_dim, dim), GELU(tanh), Linear(dim, dim))`로 생성됨 (기본 `linear`는 기존 단일 Linear). trainable mark/logging은 Sequential도 자동 호환. `train_ti2vgen_recon_overfit_256_finetuned_cam_concat_scene_ca_lora.sh`에 `model.denoiser.scene_projection=mlp` 추가.
- `src/model/denoiser/wan_ti2v.py`: `camera_input_type="channel_concat"` 모드 추가. `pose_embed(skip_embedding=True)`로 raw Plücker ray (6ch)를 latent 격자에서 받아 latent와 channel dim에서 concat. `__init__`에서 Wan TI2V의 `patch_embedding` Conv3d를 `(in_channels + 6) → dim`로 교체하고 원본 weight 복사 + 새 6채널 zero-init → 초기 동작이 Wan prior와 동일. `simple_wan_video_fn`에서 latent와 ray spatial/temporal shape 일치 검사 후 concat → 확장된 patch_embedding 통과. `_set_trainable_parameters`는 `patch_embedding.*`을 trainable로 마킹. `train_ti2vgen_recon_overfit_256_finetuned_cam_concat_scene_ca_lora.sh`: `scene_input_type=cross_attention` + `camera_input_type=channel_concat` + `camera.input_shape=[30,52]` (latent grid) + `lora.enabled=true`로 구성. text CA에 scene tokens concat, ray map은 noise와 channel concat, attention/FFN은 LoRA로 학습.
- `scripts/fast_infer_t2v.py`: 한 번 실행에 `{user_cfg, 1.0} × {user_prompt, ""}` 4가지 조합 sampling. 모델 로드 + context encode + compressor + `preprocess_batch`는 1회만 (preprocess_batch는 extrinsics in-place mutate라 반복 호출 금지). combo마다 `wrapper.model_cfg.cfg_scale`과 `batch["text"]`만 갈아끼우고 `generate_batch_with_scene` 재호출. noise_seed는 모든 combo에서 동일 → noise 고정, (cfg, prompt)만 다른 공정 비교. 출력 경로: `results/fast_infer_<exp>/<scene>/cfg{X}_prompt-{full,empty}/<sampler>.mp4`. user_cfg가 이미 1.0이면 dedup으로 2개만 생성.
- `scripts/fast_infer_t2v.py` + `fast_infer_t2v.sh`: `get_dataset(stage="test")`로 고정해서 `evaluation_index_path`가 활성화되도록 수정. 이전엔 `args.stage="train"`을 그대로 `get_dataset`에 넘겨서 `self.stage="train"`이 되어 `dataset_dl3dv.py:303`의 eval_index filter가 skip → view_sampler가 매번 random context/target 고름. shell 인자 `--stage`를 `--stage_override`로 rename (cfg.dataset.stage_override에만 영향 — 1K vs 11K pool 선택). 새 인자 `--evaluation_index_path`로 명시 가능, 미지정 시 dataset이 `(chunk_targets, val_seen)`에서 auto-resolve (`assets/evaluation_index/dl3dv_c16_{34,37}_{standard,unseen}.json`).
- `scripts/fast_infer_t2v.py`: `wrapper.sampler.log_vis = lambda *a, **kw: None`로 monkey-patch 추가. `generate_batch_with_scene`이 내부적으로 `sampler.log_vis(self.logger, ...)`를 호출하는데 Trainer 없이 instantiate한 LightningModule은 `self.logger==None`이라 AttributeError 발생하던 문제.
- `scripts/fast_infer_t2v.py` + `fast_infer_t2v.sh` — Hydra/Lightning 우회 standalone TI2V/T2V inference. `exp/<exp_name>/.hydra/config.yaml`을 `OmegaConf.load`로 직접 읽고 `T2VWrapper`를 직접 instantiate, `last.ckpt` state_dict 직접 load 후 단일 scene에 대해 batch 하나 sampling해서 mp4만 저장 (save_img=False). Hydra config composition + Lightning Trainer 초기화 + sanity val + wandb/checkpoint callback + val dataloaders 전부 skip. startup ~60-80s → ~40-50s. Sampling 자체 시간(DiT forward 50 step)은 동일. 사용: `bash fast_infer_t2v.sh` 또는 직접 `python scripts/fast_infer_t2v.py --exp_name ... --scene_id ... --prompt ...`.
- RE10K 데이터셋에도 **scene blacklist** 메커니즘 적용. `src/dataset/dataset_re10k.py`에서 `dataset_dl3dv`의 `load_blacklist`/`_resolve_blacklist_path` 헬퍼를 그대로 재사용. `DatasetRE10kCfg`에 `blacklist_path: Path | None = None` 필드 추가 (None이면 `{cfg.root}/{train|test}/blacklist.csv`). `DatasetRE10k.__init__`에서 meta.csv로 `valid_keys` 빌드 직후 blacklist set으로 key 단위 필터링하고 dropped count 출력. `build_re10k_meta` 자체는 변경 없음 — meta.csv는 cfg-agnostic하게 유지하고 필터링은 `__init__`에서만 수행해서 blacklist.csv에 key를 추가하면 meta.csv 재빌드 없이 다음 학습부터 즉시 효과. 현재 `diffusion_wrapper`의 NaN-time auto-append는 DL3DV에만 분기되어 있으므로 RE10K는 NaN 발생 → 데이터 확인 → 사용자가 수동으로 `blacklist.csv` 편집 → 학습 재시작 워크플로우.
- DL3DV scene **blacklist** mechanism. `src/dataset/dataset_dl3dv.py`에 `BLACKLIST_FILENAME = "blacklist.csv"`와 `load_blacklist` / `append_blacklist` / `_resolve_blacklist_path` 헬퍼 추가. CSV 컬럼은 `scene, reason, step, loss, detail`. `DatasetDL3DVCfg`에 `blacklist_path: Path | None = None` 필드 추가 (None이면 `{cfg.root}/train/blacklist.csv` 기본). `build_dl3dv_meta`는 scene_dir scan 직후 blacklist set으로 필터링하고 dropped count를 출력. `DatasetDL3DV.__init__`도 meta.csv 로드 직후 동일하게 chunk.name 단위로 필터 (기존 meta.csv를 재빌드하지 않고도 즉시 효과).
- `src/dataset/dataset_re10k.py`에 **camera teleport 필터** 추가. `_re10k_cameras_to_c2w(cameras)` 헬퍼가 `(N, 18)` 카메라 텐서에서 3x4 w2c 블록을 추출해 c2w로 inverse. `check_teleport_camera(extrinsics)`는 dl3dv 버전을 그대로 미러링 (non-finite, `|diff|>10` per-axis frame-to-frame jump, `||diff||>15` magnitude jump, `|t|>50` absolute translation 중 하나라도 걸리면 True). `build_re10k_meta`의 `scan(chunk_path)`이 length-mismatch 검사 통과 후 이 두 함수를 호출해 broken-camera scene을 skip하고, 결과 print에 `(skipped K length-mismatch / M bad-camera)` breakdown 표시. **기존 meta.csv는 재빌드 안 됨** — 새 필터를 적용하려면 해당 meta.csv를 지운 뒤 next dataset init 때 자동 rebuild.
- `src/model/diffusion_wrapper.py`에 `DiffusionWrapper._diagnose_batch_for_blacklist(batch, target_latents)` static method 추가. NaN loss 발생 시 batch의 context/target extrinsics·intrinsics·raw input(`latent` key)과 인코딩된 `target_latents`를 sample 단위로 검사해 anomaly 항목(reason, detail) list를 반환. 임계: `|t| > 50` (extrinsics translation, `check_teleport_camera`와 동일), raw input `|x| > 5`, encoded latent `|z| > 200`, 모든 텐서 non-finite check. `(scene, reason)` 단위로 dedup.

### Changed
- `src/model/denoiser/wan_ti2v.py`: `camera_input_type` 값 `"ac3d"` → **`"controlnet"`** 리네임 (Literal/validation/모든 비교문 + `train_ti2vgen_recon_overfit_256_finetuned_ac3d{,_full_controlnet}.sh`). 내부 모듈/변수명(`ac3d_patch_embedding`, `ac3d_blocks`, `ac3d_projectors`, `ac3d_num_layers`, `ac3d_residuals` 등)은 state_dict 키·trainable 매칭 substring이라 **유지** — config 노출 값만 변경. 이제 scene/camera 둘 다 `controlnet` 값으로 ControlNet 분기 사용을 표현. 옛 값 `ac3d`는 dacite `UnionMatchError`로 거부.
- `src/dataset/dataset_dl3dv.py:load_evaluation_index`: view_sampler가 `caption_window`이면 eval index 파일명에 `_caption` variant를 붙여 자동 선택(`dl3dv_c16_{size}_caption_{split}.json`). 기존 bounded/unbounded는 variant="" 그대로. val/test에서 caption_window면 caption-정렬 고정 index를 로드해 `sample_evaluation_index_views`로 context/target 사용 (검증: val·test 모두 caption_unseen 140 scenes 로드, sample 인덱스가 json과 정확히 일치).
- `train_ti2vgen_recon_overfit_256_finetuned.sh`: view_sampler를 `dataset/view_sampler=caption_window`로 전환 + `+dataset.load_prompts=true` 추가. caption 구간에 정렬된 연속 target window로 학습하고 해당 caption이 `sample["text"]`로 들어가게 함. `model.text_encoder=null`이지만 wan_ti2v denoiser가 내부 UMT5(`uses_internal_text_encoder=True`)로 `batch["text"]`를 인코딩해 cross-attention context로 주입하므로 text 컨디셔닝은 정상 동작 (`scripts/verify_caption_window_text.py`로 data 경로 검증 완료).
- `.gitignore`에 vendored upstream repo 3개 추가: `src/model/DiffSynth-Studio` (~417GB), `src/model/ac3d`, `src/model/GEN3C`. 각각 nested `.git`을 가진 read-only 참조라 `git add -A` 시 인덱스에 끌려가는 사고 방지 (`WorldTraj`는 이미 등록돼 있었음).
- `CLAUDE.md`에 **Git 커밋 워크플로우** 섹션 추가. 역할 분담 명문화: Claude는 코드 변경 작업마다 분기 없이 **현재 브랜치에** 변경+CHANGELOG를 하나의 커밋(`feat/fix/exp/chore: <요약>` + Co-Authored-By)으로 묶고 요약 보고 후 멈춤 (요청 없이 자동 커밋 — 기본 "요청 시에만 커밋" + "default 브랜치면 분기" 동작 override); push/merge/pull/브랜치 생성·삭제는 사용자 수동. 스테이징은 변경 파일만 명시적 `git add <경로>` (`git add -A`/`.` 금지) — vendored 디렉토리(`src/model/{DiffSynth-Studio,ac3d,GEN3C}` ~417GB, `WorldTraj`)가 인덱스에 끌려가는 사고 차단. 읽기전용/분석 작업은 커밋 미수행.
- `CLAUDE.md` 업데이트 — `/init` 결과. 코드 드리프트 반영: `camera_input_type`에 `channel_concat`/`ac3d`, `condition_latents_input_type`에 `first_frame_depth`/`first_frame_depth_soft`, 새 `scene_projection` ∈ `{linear, mlp}` 노브 추가. Models 섹션에 vendored `DiffSynth-Studio`/`ac3d`/`GEN3C` 디렉토리 (read-only 참조) 명시. Data 섹션에 dl3dv/re10k **scene blacklist** 메커니즘 (`blacklist.csv`, helper 위치, DL3DV NaN 자동 append vs re10k 수동) 추가. Commands에 `fast_infer_t2v.sh` standalone inference, env vars에 `src/main.py`의 `OMP_NUM_THREADS` setdefault 추가.
- `src/main.py`: 모든 import 이전에 `os.environ.setdefault("OMP_NUM_THREADS", "8")` + MKL/OpenBLAS 동일. PyTorch default가 모든 코어를 intra-op parallel pool로 잡아서 main process 1개가 72-core 머신에서 4000%+ CPU spike을 내고, train job 4개 동시 실행 시 core 경쟁으로 throughput이 떨어지던 문제. `setdefault`라서 shell에서 명시적으로 export하면 override 가능. 효과는 fork되는 dataloader worker에도 자동 전파. 적용 범위는 `src.main` 통해 들어오는 모든 train/infer (SceneGen `src.main_scene`은 별도 적용 필요).
- `src/model/metrics.py:compute_ssim`: skimage `structural_similarity` + Python loop + `.cpu().numpy()` 제거. 이미 import만 돼있던 `torchmetrics.functional.image.structural_similarity_index_measure`로 대체 → fully GPU. `gaussian_kernel=True, kernel_size=11, sigma=1.5, data_range=1.0`로 skimage 기본값과 매칭.
- `src/model/metrics.py:update_fid`: per-sample Python loop 제거. `(B, V, C, H, W) → (B*V, C, H, W)`로 flatten 후 `self.fid.update(..., real=True/False)`를 2번만 호출 (이전: B번 × 2). FrechetInceptionDistance.update가 batched 입력을 지원해서 안전. host↔device launch overhead 감소.
- `submodules/fvd/frechet_video_distance.py:get_activations` + `src/model/metrics.py:compute_fvd`: FVD I3D forward를 CPU → GPU로 이동. `frechet_video_distance(..., device="cuda" if available else "cpu")`로 호출, `get_activations` 안에서는 `next(model.parameters()).device`로 모델 device 감지 후 batch `.to(device)`. 활성화는 CPU/numpy로 반환되어 cov + scipy `sqrtm`은 그대로 CPU. validation 누적은 여전히 `.cpu()`이므로 GPU 메모리 압박 없음. on_validation_end에서 가장 큰 CPU 병목이던 부분 제거 → 누적 sample 수가 클수록 효과 (수 분 → 수 초 단위).
- `infer_pure_ti2v.sh`: pure Wan2.2 TI2V 5B (T2V mode) inference로 정리. `model.cfg_scale=1.0 → 5.0` (text prompt + negative prompt CFG 작동), `model.compressor=null`로 compressor module 자체를 미생성 (이전 `compressor.ckpt_path=null`만으로는 모듈 객체는 남았음), `freeze.compressor=true → false` (compressor 없는데 freeze 호출 시 AttributeError 회피). `model.compressor.input_shape`/`compressor.camera.input_shape` 라인 제거. 살아있는 path는 Wan T5 text encoder + Wan TI2V DiT (model_root에서 safetensors) + Wan VAE only.
- `src/model/t2v_wrapper.py:generate_batch_with_scene`: 진입부의 `context_latents = get_latents(...)`을 `if self.model_cfg.compressor is not None:` guard 안으로 이동. compressor=null이면 context VAE encode 자체를 skip해서 pure T2V inference에서 context view forward 비용 0. `compressor is None` 분기에서 `device/dtype`은 `batch["target"]["latent"]`에서 (없으면 `denoiser.parameters()` + extrinsics device)로 fallback. compressor가 있을 때의 기존 동작은 변화 없음.
- `train_scenetok_dl3dv_256-480_finetune.sh`: `optimizer.lr=1.0e-4` override 추가. yaml 기본 1e-5에서 10배 상향 — TI2V 측 shell (`train_ti2vgen_recon_overfit_256_finetune_all.sh`)이 yaml lr 기본 1e-4를 그대로 쓰는 것과 학습 dynamics를 일치시키기 위함.
- `src/model/diffusion_wrapper.py:training_step` NaN loss handler: 기존 "3회 누적 → `breakpoint()`" 동작을 제거하고, **NaN 발생할 때마다** `_diagnose_batch_for_blacklist`로 진단 → 데이터-쪽 anomaly가 잡히면 `dataset_dl3dv.append_blacklist`로 `{cfg.root}/train/blacklist.csv`에 append (scene 단위 dedup). 데이터셋이 `dl3dv`가 아닌 경우엔 진단만 로그하고 blacklist는 skip. 그래디언트 NaN guard(line ~1144)가 이미 optimizer step을 skip하므로 학습은 그대로 continue — auto-mode 친화적. `nan_loss_count`는 lifetime counter로 유지 (log 용).
- `CLAUDE.md` Commands → A. SceneTok 목록을 현재 레포의 실제 shell 파일명·exp_name으로 갱신. `train_scenetok_256.sh`/`train_scenetok_re10k.sh`/`train_scenetok_wan.sh`는 더 이상 존재하지 않으며 `train_scenetok_{dl3dv_256, dl3dv_wan_256, dl3dv_wan_480, re10k_256, re10k_vavdc, chunked}.sh`로 분기된 상태를 반영. `train_scenetok.sh` 설명도 잘못된 "DL3DV wan-wan scratch"에서 실제(`scenetok_va-vdc_re10k_scratch`)로 정정.
- `src/model/diffusion_wrapper.py:training_step`: **loss NaN/Inf counter + 3회 누적 시 `breakpoint()`** 추가. `self.nan_loss_count`를 `__init__`에서 0으로 초기화 (process-lifetime 누적), `loss.mean()` 직후에 `isnan/isinf` check → 발생 시 카운터++ + step·batch_idx·scenes를 stdout으로 출력, 3회째 발생 시 pdb 진입해 model state·batch·scene을 인터랙티브 inspect 가능. 기존 gradient NaN handler(`line 1144`)는 gradient만 skip하지 loss 자체의 발산 빈도/원인을 알 길이 없었던 gap을 메움. `continue`로 빠져나오면 학습 그대로 진행 (해당 step은 gradient guard가 자동 skip).
- `src/model/diffusion_wrapper.py:validation_step`: `self.generated`/`self.predicted`에 sample append 직전에 **NaN/Inf finite-mask guard + 진단 print** 추가. NaN/Inf가 있으면 count·min·max 출력 후 해당 sample들을 batch에서 drop, 전체가 NaN이면 batch 통째로 skip (return None). 학습 측 `Skipping Nans!` (line 1144)는 gradient만 skip하지 forward output을 sanitize하지 않아서 validation에 NaN이 그대로 흘러들어가 FVD `linalg.sqrtm`이 NaN-poisoned cov 위에서 Schur 분해 무한 spin으로 hang하던 진짜 root cause 대응. (`NaN + ε == NaN`이라 이전 ε regularization patch는 NaN-poisoned case에서 무력했음.)
- `src/model/metrics.py:update_fid/update_fvd`: 진입 시점에 finite-mask 필터 추가 (defense in depth). non-finite sample 발견 시 drop 개수 출력 후 제외, 전체 drop이면 silent skip. 위 source-side guard와 별개로 미래 다른 호출 경로(test_step, predict_step 등)에서 NaN이 들어와도 안전.
- `submodules/fvd/frechet_video_distance.py`: `calculate_fvd_from_activations`의 `linalg.sqrtm` 전 ε·I regularization 재부활 (이전 revert에서 빠졌던 패치). default ε `1e-10 → 1e-6`로 강화하고 무조건적으로 한 번 적용 후 sqrtm 호출. NaN/Inf fallback은 ε=1e-3로 재시도. 32 sample × 400-dim I3D feature → rank-deficient cov → scipy sqrtm의 Schur 분해 무한 spin (NaN이 아니라 진짜 hang)으로 train_scenetok_re10k_256 validation이 멈추던 문제 대응. FVD 절대값 변화는 ~1e-3 (negligible, sample-size << feature-dim에서 오는 estimator noise보다 작음). 모델 간 ranking은 보존.
- `src/dataset/dataset_re10k.py`: `__getitem__`의 `view_sampler.sample(...)` 호출을 try/except로 감싸 `ValueError`를 `return None`으로 graceful skip. 위 unbounded sanity-check 추가, scene별 frame 부족, OOB 인덱스 등 sampler 실패 케이스를 dataset 단에서 흡수해 worker crash 방지. `safe_collate` + step methods의 None guard와 결합해 None-safe path가 완성됨.
- `src/model/diffusion_wrapper.py`, `src/model/t2v_wrapper.py`: 모든 step 메서드(`training_step`, `validation_step`, `test_step`) 최상단에 `if batch is None: return None` guard 추가. `safe_collate`가 batch 전체 None 시 `None`을 반환하는 새 동작에 맞춰 — Lightning automatic optimization은 None을 반환하면 backward/optimizer.step()을 skip, validation_step은 이미 코드베이스에서 None 반환을 정상 패턴으로 쓰고 있어서 안전. DDP 동기화는 현재 모든 학습 스크립트가 `trainer.devices=1`이라 영향 없음. (T2VWrapper는 DiffusionWrapper subclass라 validation_step은 부모 guard 상속.)
- `src/dataset/data_module.py`: `safe_collate`에 `batch = [b for b in batch if b is not None]` 사전 필터 복원 + 전체가 None이면 `None` 반환 (caller가 skip 가능). val/test/predict dataloader에도 `collate_fn=safe_collate` 추가. 이유: `dataset_re10k.py.__getitem__`이 bad baseline / 잘못된 shape에서 `None`을 반환하는 경로(line 156, 174)는 그대로 살아있는데, 현재 `safe_collate`가 None을 사전 처리하지 않아 `default_collate`가 `'NoneType' object is not subscriptable`로 crash했음. 이전 revert로 train만 `safe_collate`를 쓰던 상태였는데, re10k가 val/test에서도 같은 `__getitem__`을 거치므로 양쪽 모두 None-safe로 통일.
- `config/experiment/custom/scenetok_va-wan_shift8_dl3dv_scratch_wide.yaml`: context/target shape을 256×448로 통일. `dataset.context_shape` `[256, 256] → [256, 448]`, `dataset.target_shape` `[480, 832] → [256, 448]`. 그에 맞춰 derived shapes도 조정: `model.denoiser.input_shape` `[30, 52] → [16, 28]`, `model.denoiser.camera.input_shape` `[240, 416] → [128, 224]`, `model.compressor.input_shape` `[16, 16] → [16, 28]`, `model.compressor.camera.input_shape` `[128, 128] → [128, 224]`. wandb tag도 `480x832 → 256x448`.

### Added
- `scripts/probe_lpips_fp16.py` — LPIPS metric이 학습 중 같은 값으로 수렴하는 증상 진단용 probe. (1) noise scale 0.001–0.05에서 fp32/fp16 LPIPS 비교 — fp16이 정상으로 monotonic하게 변하고 fp32와 1e-4 이내 일치하는 것 확인. (2) lin weight nonzero ratio 출력 — fp16 캐스트 후에도 underflow 없음(`14716138/14716160 ≈ 1.0`). (3) `tmp/wan_vae_recon/`의 real Wan VAE recon 10쌍에 대해 `Metric()`을 wrapper-동일 setup(`freeze` + `convert_to_buffer`)으로 instance화해서 bf16 입력으로 `compute_lpips` 호출 — per-scene `0.0123~0.0447`로 정상 변화, aggregate `0.024292`가 fp32 평균(`0.024125`)과 0.0002 이내 일치. 결론: **compute_lpips 함수 자체는 정상**. 남은 가설은 LightningModule의 precision-cast가 LPIPS buffer를 다운캐스트하는지 (H6).
- `scripts/dump_va_vae_recon.py` — VA-VAE (`checkpoints/vavae-imagenet256-f16d32-dinov2.pt`, f=16, latent_channels=32) 버전의 recon dump. 동일한 10개 re10k test scene을 **세 해상도**(256×256, 256×448, 480×832)에서 encode→decode 후 `tmp/va_vae_recon/`에 저장. 입력은 `(B,C,H,W)`, `(x*2-1)`로 [-1,1] 정규화 후 `.latent_dist.mode()`로 deterministic latent 사용. 결과: 256×256은 PSNR **31.40 dB** / L1 0.0176, 256×448은 33.67 dB / 0.0141, 480×832는 37.70 dB / 0.0088 — PSNR이 latent 토큰 수(256→448→1560)와 monotonically 비례. **VA-VAE의 학습 native 256×256에서도 recon이 가장 나쁨** (학습 해상도 근접성보다 f=16 bottleneck의 spatial 토큰 수가 dominant). Wan VAE도 같은 방향 (256×448→480×832에서 PSNR 6.4 dB 갭). VA-VAE는 raw latent magnitude가 큼 (z_std≈3.5, |z|max 최대 30) → 학습 경로의 `latent_mean`/`latent_std` 정규화 필요. |z|max는 큰 해상도에서 오히려 더 큼 → 작은 해상도 outlier 가설은 양 VAE에서 모두 지지되지 않음.
- `scripts/dump_wan_vae_recon.py` — re10k test 10 scene을 두 해상도(256×448, 480×832)에서 Wan2.2 VAE로 encode→decode 후 `tmp/wan_vae_recon/`에 `scene{NN}_{key}_{HxW}_{orig,recon}.png`로 저장. 학습 경로와 동일하게 `inputs * 2 - 1`로 [-1,1] 정규화 후 인코딩. scene별 PSNR/L1/z_std/|z|max와 해상도별 aggregate를 stdout으로 출력. 결과: 256×448은 PSNR 40.70 dB / L1 0.0062, 480×832는 47.07 dB / 0.0029로 **작은 해상도에서 recon 충실도가 일관되게 낮음** (latent z 통계는 두 해상도 거의 동일).
- `scripts/check_wan_vae_outliers.py` — Wan2.2 VAE에 re10k test scene N개를 통과시켜 latent의 NaN/Inf, `|mean|`, std, max|z|, kurtosis를 검사하고 per-channel std 분포까지 출력하는 진단 스크립트. `sys.path`를 자동으로 repo root에 추가하므로 어느 cwd에서 실행해도 동작 (단 데이터/ckpt 경로는 repo root 기준이라 거기서 실행 권장). 사용: `python scripts/check_wan_vae_outliers.py [num_scenes] [h,w]`.

### Changed
- `src/model/diffusion_wrapper.py:on_validation_end` — `self.metric(...)` 호출 직전에 LPIPS 내부 weight/buffer dtype과 pred/gt dtype을 출력하는 **임시 debug print** 추가. LPIPS metric이 학습 중 다른 모델들 간에 같은 값(예: 0.0244)으로 수렴하는 증상의 root cause를 H6(Lightning precision-cast가 LPIPS fp16 buffer를 bf16/fp32로 다운캐스트해서 VGG feature가 평탄해짐) vs 구조적 saturation으로 가려내기 위함. 진단 끝나면 제거 예정. 출력 예: `[LPIPS dtype] params=set() buffers={torch.float16} pred=torch.bfloat16 gt=torch.bfloat16`.
- `CLAUDE.md` 업데이트 — `/init` 결과. Commands 섹션을 SceneTok 학습 (`train_scenetok*.sh`, 현재 active line) / TI2V denoiser 학습의 두 그룹으로 재구성. Configs 섹션의 active custom YAML 목록을 현재 브랜치 상태 (`scenetok_{va-vdc,wan-wan}_lognorm_re10k_scratch.yaml` 외)로 갱신. Data 섹션에 re10k `meta.csv` 빌더, `{standard,unseen}` evaluation index split 자동 선택 (`(chunk_targets, val_seen)` 키), unbounded sampler `offset`/`chunk_targets` 동작 추가. 새 "Known gotchas" 서브섹션 — Wan2.2 VAE + 1024 scene_tokens scratch 학습의 `kl_weights` anchor 단계적 상향 패턴, FVD `sqrtm` hang 회피 노트.
- `src/dataset/dataset_re10k.py`를 **원본 대비 4가지 핵심 호환성 수정 + meta.csv build/load**만 남도록 최소화. (1) `stage == "train"`은 `root/train` 사용 (val/test는 `root/test`); (2) `view_sampler.index` 강제 의존을 `hasattr(view_sampler, "index")` 분기로 완화 → unbounded/bounded 등 training sampler에서도 동작; (3) `view_sampler.sample()` 호출에 `stage`, `extrinsics` 인자 추가 (training sampler 필요); (4) sample 반환 타입 (tuple / list / 단일 `ViewIndex`) 정규화. 추가로 `build_re10k_meta` 함수 — `len(images) == len(cameras) AND ≥ min_frames(=37)` scene만 `meta.csv` (chunk, key, num_images)에 기록 (timestamps는 안 씀, shape 검증도 안 함). `__init__`에서 meta.csv build/load 후 `valid_keys`로 scene 필터.
- 제거된 기능 (`dataset_re10k.py`): `DatasetRE10kCfg`의 `context_shape`/`target_shape`/`evaluation_index_path`/`val_seen`/`min_frames` 필드, `load_evaluation_index`/`sample_evaluation_index_views` 메서드, asymmetric crop 분기. 모두 이 세션 도중 추가됐다가 미사용/부분 사용으로 판명 → 원본 미니멀 형태로 회귀.
- `src/dataset/data_module.py`: `val_dataloader`에서 `dataset_cfg.name in {"dl3dv", "re10k"}` 분기를 다시 `dl3dv`만으로 축소 → re10k는 `key == "unseen"` 시 dataloader가 silently skip되는 원본 동작 복귀 (`val_seen` 필드 제거에 따른 정리).
- `config/experiment/custom/scenetok_wan-wan_lognorm_re10k_scratch.yaml` / `scenetok_va-vdc_lognorm_re10k_scratch.yaml`: 위 cfg 필드 제거에 맞춰 yaml에서 `context_shape`/`target_shape`/`min_frames` 줄 정리. `dataset.shape`만으로 표현.
- `DATA/re10k/re10k/{train,test}/meta.csv` 재생성 — 새 단순 builder 기준. train 61,036 scenes, test 6,748 scenes 통과 (이전 timestamps+shape 기반 빌드: 60,936 / 6,723와 비교해 약간 차이).

### Reverted
- 보조 안전망 패치들을 원본대로 되돌림 (사용자 요청):
  - `submodules/fvd/frechet_video_distance.py`: `calculate_fvd_from_activations`의 `linalg.sqrtm` 전 `1e-6·I` regularization 삭제 → 원래 단순 `sqrtm(f_cov.dot(s_cov))` 호출 + `isfinite` fallback만 유지.
  - `src/dataset/data_module.py`: `safe_collate`의 `batch = [b for b in batch if b is not None]` 사전 필터 제거. val/test/predict dataloader의 `collate_fn=safe_collate` 인자도 제거 (원래 train만 safe_collate 사용).
  - `src/dataset/dataset_re10k.py` `__getitem__`: view_sampler가 `ValueError` 던질 때 `return None`으로 감싸던 try/except 제거 → 다시 raise. `indices.max() >= len(example["images"])` 사전 검사도 제거.

### Changed
- `submodules/fvd/frechet_video_distance.py`: `calculate_fvd_from_activations`에서 `linalg.sqrtm` 호출 전에 cov에 `1e-6·I` regularization을 항상 적용. 32 sample × 수백 차원 I3D feature → rank-deficient cov → scipy `sqrtm`의 Schur 분해가 무한 스핀하던 validation hang을 회피. NaN/Inf 나오면 ε을 1e-3로 한 번 더 키워 retry.
- `config/experiment/custom/scenetok_wan-wan_lognorm_re10k_scratch.yaml`: re10k 256×448 wan-wan scratch가 step ~2300 부근에서 `compressor.scene_tokens` gradient NaN으로 발산하던 문제 대응. compressor에서 `init_large: false` + `init_small: true` (scene_tokens 초기 std 5.0 → 0.02). published va-vdc re10k는 num_scene_tokens=512인데 우리는 1024로 두 배라 같은 1e-10 KL anchor로는 부족.
- `config/experiment/custom/scenetok_wan-wan_lognorm_re10k_scratch.yaml`: `kl_weights`를 1e-7 → 1e-6 (10×)로 추가 상향. 1e-7로 11000 step까지는 안정했으나 step ~13475부터 cascading gradient spike(`Skipping Nans` 빈도 ~18%)가 발생. 1024-token scene_tokens 분포가 late-stage에서도 안정 유지되려면 KL anchor가 더 필요.
- `config/experiment/custom/scenetok_wan-wan_lognorm_re10k_scratch.yaml`: `kl_weights`를 1e-6 → 1e-5 (또 10× 강화). 1e-6로도 step ~20000에서 같은 cascading 재발 (loss가 0.3→60+로 폭발). 누적: 1e-10(published) → 1e-7(step 13475 발산) → 1e-6(step 20000 발산) → 1e-5. 1024-token scratch 학습 안정화에 필요한 anchor 강도.
- `src/dataset/data_module.py`: `test_dataloader`의 `get_dataset` 호출에서 `force_shuffle=True` → `False`로 수정 (val/predict와 대칭, evaluation 결과 비교용 deterministic order 보장). 추가로 `safe_collate`가 `None` 샘플을 사전에 필터링하고 val/test/predict dataloader도 `collate_fn=safe_collate` 사용 — re10k loader가 bad baseline/이미지 shape mismatch로 `None`을 반환할 때 `default_collate`의 `'NoneType' object is not subscriptable` crash 회피.
- `src/dataset/dataset_re10k.py`: 원본 raw frame shape 체크를 `(3, 360, 640)` strict 비교로 되돌림. 큰 raw frame은 downstream `apply_crop_shim`이 rescale+center-crop으로 정확히 잘라주는 것은 그대로지만, 학습 분포의 일관성을 위해 native 해상도가 아닌 scene은 제외. view sampler가 ValueError 던질 때 (예: scene이 짧아 Wan 4N+1 요구 raw 프레임 수 부족) crash 대신 `return None`으로 graceful skip — safe_collate가 걸러줌. `build_re10k_meta`에도 frame shape 검증 추가 — scene의 **모든 frame**이 PIL `size`가 `(640, 360)`인 경우에만 meta.csv에 기록 (기존 frame-count 필터와 함께 적용). 첫 frame만 체크하면 scene 내부에 다른 해상도 frame이 섞여 있을 때 getitem strict 체크에서 None 반환됨.
- `src/dataset/dataset_re10k.py`: __getitem__에서 `indices.max() >= len(example["images"])`인 경우 graceful skip (return None) 추가. 일부 scene이 `len(timestamps) != len(images)`라 meta.csv num_images 기준으로 통과돼도 indexing 시 IndexError 나던 케이스 차단. 같은 맥락으로 `build_re10k_meta`도 `n = min(len(timestamps), len(images))`로 보정 (이전 meta.csv를 새로 빌드하면 자동 적용; rebuild 없어도 runtime guard로 안전).
- `src/dataset/dataset_re10k.py`: dl3dv 패턴 따라 `build_re10k_meta` 추가. 각 split(`train`/`test`)의 모든 `.torch` chunk를 한 번 스캔해서 `meta.csv`(컬럼 `chunk, key, num_images`)를 쓰고, 이후 init에선 그 csv만 읽어 `num_images >= cfg.min_frames`(기본 37)인 scene만 학습/eval에 사용. `DatasetRE10kCfg.min_frames` 신규 필드 (default 37 = Wan 4N+1 + 10 latent 단일 chunk 요구치). 빌드는 첫 init 시 자동 (8 workers, ThreadPoolExecutor + tqdm), 이미 존재하면 read-only. re-build 필요시 `meta.csv` 삭제.
- `src/dataset/dataset_dl3dv.py`: `load_evaluation_index`가 `(chunk_targets, val_seen)` 조합으로 `dl3dv_c16_{34|37}_{standard|unseen}.json`을 명시적으로 선택. 기존 단일 파일 + 1K/11K chunk-prefix 필터 트릭을 대체 — dl3dv도 re10k와 동일한 split-file 패턴으로 통일. 명시적으로 `evaluation_index_path`를 지정한 config는 그대로 유지.
- `assets/evaluation_index/dl3dv_c16_{34,37}_{standard,unseen}.json` 재생성 — meta.csv 순서대로 첫 140 scene씩 픽 (`1K` prefix → standard, `11K` → unseen). 5/20 commit `d704588`(`load_evaluation_index` 추가) 이전 wandb run과 동일한 standard scene 순서 (`a4c20..., aeea98..., 97f72...`)로 복귀. target은 `[0..T-1]`, context는 target 범위에 균등 16개. (이전엔 `dl3dv_c16_37.json` 원본 280 key를 meta prefix로 split했으나, 그 원본 자체가 meta.csv 순서와 무관해서 prior run의 scene과 안 맞았음.)
- `assets/evaluation_index/re10k_c12_{34,37}_{standard,unseen}.json` 재생성 — `meta.csv` 순서대로 train 첫 140 scene(standard), test 첫 140 scene(unseen). target `[0..T-1]`, context 균등 12개.
- `config/experiment/custom/scenetok_va-vdc_lognorm_re10k_scratch.yaml` 정리 — 원본은 `context_root`/`target_root`/`map_dict` (precomputed latents path) 가정이라 우리 raw-image 기반 `DatasetRE10k`와 호환 안 됨. raw image 입력 경로(`root: ./DATA/re10k/re10k`)로 전환, view_sampler에 `num_context_views=16, num_target_views=10, target_split_prob=1.0, num_target_split=1, offset=0, chunk_targets=false` 명시(unbounded default 0 회피). VideoDCAE temporal_downsample=4의 4N 패턴 (Wan 4N+1 아님)이라 `min_frames: 40`으로 설정. context/target shape `[256, 256]` published 기본 유지, 비교 실험 시 CLI override 가능.
- `src/dataset/dataset_dl3dv.py`: `load_evaluation_index`에서 `evaluation_index_path`가 미지정일 때 view sampler의 `chunk_targets` 값을 보고 자동으로 fallback 경로 선택 — `chunk_targets=true`면 `assets/evaluation_index/dl3dv_c16_34.json`, `false`면 `assets/evaluation_index/dl3dv_c16_37.json`. 명시적으로 path를 지정한 기존 config는 그대로 유지.
- `config/experiment/custom/scenetok_wan-wan_lognorm_re10k_scratch.yaml` 및 `train_scenetok_re10k.sh`: re10k wan-wan lognorm 학습 셋업으로 정정 (이전엔 dl3dv 내용이 잘못 복붙되어 있었음). dataset=re10k + unbounded view sampler. 해상도 256×448로 통일 (encoder/decoder 모두) → denoiser/compressor latent `[16, 28]`, camera input `[128, 224]`. scheduler weighting=`logit_normal` + `random_chunked_uniform`, cfg_train=true, compressor `kl_weights=[1e-10, 1e-10]`로 va-vdc lognorm re10k와 라인 맞춤. `batch_size=4`. Wan VAE 4N+1 정합을 위해 view_sampler에 `offset: 1, chunk_targets: false, num_target_views: 10, num_target_split: 1` 설정 (디폴트는 chunking off) → step당 1 chunk × raw 37 / latent 10. `chunk_targets: true, num_target_split: 2` override 시 2 chunks × raw 17 / latent 5 (총 raw 34 / latent 10) 모드로 전환 가능.
- `src/dataset/view_sampler/view_sampler_unbounded.py`: bounded sampler와 동일한 `offset`/`chunk_targets` 인지 분기 추가. `offset != 0`이면 per-chunk Wan 4N+1 레이아웃 (chunk당 latent N → raw 4N-3 프레임). `chunk_targets`는 sampler 레이아웃에 영향 없고, downstream wrapper의 raw→latent 계산 경로 선택용. 기존 `offset=0` 동작은 그대로.
- `src/dataset/dataset_re10k.py`: `DatasetRE10kCfg`에 옵셔널 `context_shape`/`target_shape`, `evaluation_index_path` 필드 추가. `__init__`을 dl3dv 패턴으로 정리 — train stage는 `root/train/index.json`, val/test stage는 `root/test/index.json`을 통해 scene→chunk 매핑 (양쪽 모두 index.json 사용). `__len__`은 scene 수, `__getitem__`은 deterministic하게 scene별 chunk를 로드. val/test에서는 dl3dv처럼 사전 샘플링된 `evaluation_index`(view_sampler 무관)로 context/target 인덱스 고정 — `evaluation_index_path` 미지정이면 view_sampler.cfg.chunk_targets 보고 자동 fallback: `chunk_targets=true`면 `assets/evaluation_index/re10k_c12_34.json`, false면 `assets/evaluation_index/re10k_c12_37.json`. 비대칭 `context_shape`/`target_shape` 지원은 그대로.
- `assets/evaluation_index/re10k_c12_34.json`, `assets/evaluation_index/re10k_c12_37.json` 신규 추가 — re10k test set에서 ≥37 프레임인 scene 280개에 대해 context 12개 (target 범위에 균등 분포) + target [0..T-1] (T=34 또는 37) 사전 샘플. seed=42. (백워드 호환용으로 보존되지만 새 학습에선 아래 `_standard`/`_unseen` split 파일이 자동 선택됨.)
- `src/dataset/dataset_re10k.py` + `src/dataset/data_module.py`: re10k도 dl3dv처럼 `val.standard` / `val.unseen` 두 validation loader 분기 지원. `DatasetRE10kCfg.val_seen`이 `True`면 `DATA/re10k/re10k/train`(seen pool), `False`면 `.../test`(unseen pool)을 사용. `data_module.py:148`의 dl3dv 전용 분기에 re10k 추가 → `val.unseen` dataloader가 더이상 silently skip되지 않음. `load_evaluation_index`는 `(chunk_targets, val_seen)` 조합으로 `re10k_c12_{34|37}_{standard|unseen}.json`을 자동 선택.
- `assets/evaluation_index/re10k_c12_{34,37}_{standard,unseen}.json` 신규 — standard 140 train scene + unseen 140 test scene, ≥37 frame 필터, seed=42. 같은 scene set을 c34/c37 양쪽이 공유 → chunked/non-chunked 모드 비교 시 scene 변동 없음.
- `train_scenetok_256.sh`: dl3dv wan-wan scratch에서 `kl_weights=[0,0]`로 인해 compressor의 KL 분포가 발산해 NaN 터지는 문제 대응 — `model.compressor.kl_weights=[1e-10,1e-10]` CLI override 추가. published config는 보존.
- `train_scenetok_256.sh`: 256x448 입력/출력 학습에 맞춰 denoiser/compressor latent grid를 `[16,28]`, camera input grid를 `[128,224]`로 override.
- `src/dataset/dataset_dl3dv.py`: context/target preprocessing 경로를 통일하고 출력 image shape가 요청한 `context_shape`/`target_shape`와 다르면 즉시 에러를 내도록 검증 추가.
- `train_scenetok.sh` (`scenetok_wan-wan_dl3dv_480_scratch_large`, 480×832): `data_loader.train.batch_size=2`로 낮추고 `trainer.accumulate_grad_batches=2`를 추가해 effective batch는 유지하면서 첫 training step의 backward activation OOM(B200, RoPE `rotate_half` 경로에서 발현되던 244 MiB 추가 할당 실패)을 회피.
